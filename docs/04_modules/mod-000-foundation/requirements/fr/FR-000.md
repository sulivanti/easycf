> ⚠️ **ARQUIVO GERIDO POR AUTOMAÇÃO.**
>
> - **Status DRAFT:** Enriqueça o conteúdo deste arquivo diretamente.
> - **Status READY:** NÃO EDITE DIRETAMENTE. Use a skill `create-amendment`.
>
> | Versão | Data       | Responsável | Status/Integração |
> |--------|------------|-------------|-------------------|
> | 0.1.0  | 2026-03-15 | arquitetura | Baseline Inicial (forge-module) |
> | 0.2.0  | 2026-03-15 | AGN-DEV-03  | Enriquecimento FR (enrich-agent) |
> | 0.3.0  | 2026-03-17 | AGN-DEV-03  | Adição FR-015/016/017/018 (MFA, SSO, Recovery, Apple); alinhamento scopes 3-seg; fix evento FR-008 |
> | 0.9.8  | 2026-03-30 | merge-amendment | Merge FR-000-M02: 2 novos endpoints Users API — POST /users/:id/reset-password (admin reset senha) e DELETE /users/:id/invite (cancelar convite pendente). Ref: spec-mod002-next-steps.md, UX-001-C03. |
> | 0.9.7  | 2026-03-25 | merge-amendment | Merge FR-000-C07: 5 use cases pós-auth propagam request.session.tenantId em domain events (change-password, logout, update-profile, create-role, update-role). |
> | 0.9.6  | 2026-03-25 | merge-amendment | Merge batch FR-000-C02/C03/C05/C06: FR-004 shape /auth/me corrigido (tenant objeto), entry point rewrite (30 routes), JWT tenant/scopes no LoginUseCase, forceLogout para redirect loop. |
> | 0.9.2  | 2026-03-25 | merge-amendment | Merge FR-000-C04: mapeamento camelCase→snake_case nas rotas de auth (FR-001/FR-003/FR-004). FR-003: schema `refreshResponse` separado sem campo `user`. Ref: spec-fix-auth-route-response-mapping. |
> | 0.9.1  | 2026-03-25 | merge-amendment | Merge FR-000-C01: seed-admin.ts alinhado com catálogo canônico DOC-FND-000 §2.2 — tenants:tenant→tenants:branch, +7 scopes MOD-000 faltantes, 63 scopes totais. |
> | 0.9.0  | 2026-03-25 | merge-amendment | Merge FR-000-M01: DTO gaps Users API — role_id/role_name em UserListItem, invite_token_expired em UserDetail, mode/role_id em CreateUserRequest. Derivados: DATA-000-M01, INT-000-M01, SEC-000-M01. |
> | 0.8.0  | 2026-03-18 | usuário     | FR-003: refresh token rotation obrigatória (PENDENTE-002 opção B, OAuth2 BCP) — reuse detection + grace period |
> | 0.7.0  | 2026-03-18 | AGN-DEV-09  | FR-016: adição fluxo Identity Linking SSO (ADR-004, PENDENTE-001 opção B) — detecta email → pede senha → vincula |
> | 0.6.0  | 2026-03-18 | usuário     | Fix AVS validate-all: event names FR-009 alinhados com DATA-003 (added/removed/blocked), FR-014 storage event corrigido |
> | 0.5.0  | 2026-03-18 | usuário     | FR-006: adição endpoint users_invite_resend (POST /users/:id/invite/resend) + Gherkin |
>
| 0.4.0  | 2026-03-17 | AGN-DEV-03  | Adição FR-019 (Gates CI); Gherkin adicionado a FR-004/005/008/009/010/011/012/013/014 |

# FR-000 — Requisitos Funcionais do Foundation

---

## FR-001 — Autenticação Nativa (Login/Logout)

- **Descrição:** O sistema DEVE prover login nativo com e-mail e senha, emitindo JWT com sessionId ancorado em banco. Logout revoga sessão imediatamente.
- **Prioridade:** Must
- **Perfis impactados:** Todos os usuários
- **Done funcional:**
  - POST /auth/login aceita `{email, password, remember_me?, device_fp?}`
  - Retorna `{access_token, refresh_token, token_type, expires_in, user}` — resposta snake_case (mapeamento camelCase→snake_case na rota, FR-000-C04)
  - Seta cookies httpOnly (accessToken + refreshToken)
  - Cria registro em `user_sessions` com isRevoked=false
  - Emite evento `auth.login_success` com correlation_id
  - POST /auth/logout marca sessão como isRevoked=true
  - **Domain events tenant_id:** Login é pré-auth → `SYSTEM_TENANT_ID`; Logout é pós-auth → `request.session.tenantId` propagado via Input (FR-000-C07)
- **Efeito colateral?** [x] Sim — Idempotency-Key obrigatório (TTL 30s)
- **Dependências:** BR-001, BR-002, BR-003, BR-008, BR-010, BR-012, DATA-000, SEC-000
- **TTL de Sessão:** Normal=12h, Estendida (remember_me)=30d

```gherkin
Funcionalidade: Autenticação Nativa

Cenário: Login bem-sucedido sem MFA
  Dado que existe usuário com email "joao@empresa.com" e status ACTIVE
  Quando POST /auth/login com credenciais válidas
  Então status 200 com access_token, refresh_token, user
  E sessão criada em user_sessions
  E evento auth.login_success emitido

Cenário: Logout
  Dado que o usuário está autenticado
  Quando POST /auth/logout
  Então sessão marcada isRevoked=true
  E evento auth.logout emitido
```

---

## FR-002 — Gerenciamento de Sessões (Kill-Switch)

- **Descrição:** O sistema DEVE permitir listar sessões ativas, revogar sessão individual e revogar todas as sessões (kill-switch global).
- **Prioridade:** Must
- **Perfis impactados:** Usuário autenticado, Administrador
- **Done funcional:**
  - GET /auth/sessions — lista sessões ativas do usuário
  - DELETE /auth/sessions/:id — revoga sessão específica (204)
  - DELETE /auth/sessions — revoga TODAS as sessões do usuário
  - Eventos: `session.revoked`, `session.revoked_all`
- **Efeito colateral?** [x] Sim
- **Dependências:** BR-002, FR-001, DATA-000, SEC-000

```gherkin
Funcionalidade: Kill-Switch de Sessões

Cenário: Revogar sessão específica
  Dado que o usuário tem múltiplas sessões
  Quando DELETE /auth/sessions/:sessionId
  Então apenas essa sessão deve ser revogada (204)

Cenário: Revogar todas as sessões
  Quando DELETE /auth/sessions
  Então TODAS as sessões do usuário são revogadas
  E evento session.revoked_all emitido
```

---

## FR-003 — Renovação de Token (Refresh) com Rotação

- **Descrição:** O sistema DEVE permitir renovação de access_token via refresh_token com **rotação obrigatória** (PENDENTE-002, OAuth2 BCP). A cada refresh, novo refresh_token é emitido e o anterior é invalidado. Reuse detection invalida toda a família de tokens.
- **Prioridade:** Must
- **Perfis impactados:** Todos os usuários autenticados
- **Done funcional:**
  - POST /auth/refresh aceita cookie refreshToken
  - Valida sessão ativa em banco (isRevoked=false)
  - **Rotação:** Emite novo `refresh_token`, invalida o anterior, retorna `{access_token, refresh_token, token_type, expires_in}` (200) — **sem campo `user`** (schema `refreshResponse` separado do `loginResponse`; frontend já possui dados do user via login ou GET /me)
  - **Token families:** Cada refresh_token pertence a uma família (raiz = login original). Armazenar `token_family_id` em `user_sessions` ou tabela dedicada
  - **Reuse detection:** Se refresh_token já utilizado for apresentado → invalida TODA a família (kill-switch automático) → emite evento `auth.token_reuse_detected` (sensitivity_level=2) → retorna 401
  - **Grace period:** Token antigo aceito por 10s após rotação (mitiga race conditions em múltiplas abas)
  - Sessão revogada → retorna 401
- **Efeito colateral?** [x] Sim — emite novo refresh_token, invalida anterior
- **Timeline/Notifications?** [x] Sim — evento `auth.token_reuse_detected` → DATA-003, SEC-002
- **Dependências:** BR-002, FR-001, DATA-003, SEC-000, SEC-002

```gherkin
Funcionalidade: Refresh Token com Rotação

Cenário: Renovação com sessão válida — rotação
  Dado que o access_token expirou mas a sessão é válida
  Quando POST /auth/refresh com cookie refreshToken
  Então status 200 com access_token, refresh_token, token_type, expires_in
  E resposta NÃO contém campo "user"
  E refresh_token anterior é invalidado

Cenário: Renovação com sessão revogada
  Dado que a sessão foi revogada via Kill-Switch
  Quando POST /auth/refresh
  Então status 401

Cenário: Reuse detection — token reutilizado
  Dado que o refresh_token já foi utilizado em uma rotação anterior
  E o grace period de 10s expirou
  Quando POST /auth/refresh com o token antigo
  Então status 401
  E TODA a família de tokens é invalidada (kill-switch)
  E evento auth.token_reuse_detected emitido (sensitivity_level=2)

Cenário: Grace period — múltiplas abas
  Dado que o refresh_token foi rotacionado há menos de 10s
  Quando POST /auth/refresh com o token antigo (dentro do grace period)
  Então status 200 com novo access_token E novo refresh_token
```

---

## FR-004 — Perfil do Usuário Autenticado (/auth/me)

- **Descrição:** O sistema DEVE prover endpoint para consulta e edição do perfil do usuário autenticado.
- **Prioridade:** Must
- **Perfis impactados:** Todos os usuários autenticados
- **Done funcional:**
  - GET /auth/me retorna: `name` (string), `email`, `avatar_url`, `tenant: { id, name }` (objeto), `scopes[]`
  - O campo `tenant` DEVE ser objeto com `id` e `name` (não escalar `active_tenant_id`) — necessário para ProfileWidget e WelcomeWidget
  - O campo `name` DEVE mapear de `content_users.full_name` (não expor `full_name` ao frontend)
  - PATCH /auth/me permite edição de campos do perfil (nome, avatar) — evento `user.profile_updated` usa `tenant_id = request.session.tenantId` (pós-auth; FR-000-C07)
  - Nunca retorna passwordHash ou mfaSecret
  - A rota DEVE estar registrada no entry point (`index.ts`) — sem registro, retorna 404 quebrando sidebar/dashboard/header
- **Efeito colateral?** [x] Sim (PATCH)
- **Timeline/Notifications?** [ ] Não
- **Dependências:** FR-001, DATA-000, SEC-000

```gherkin
Funcionalidade: Perfil do Usuário Autenticado

Cenário: Consulta de perfil
  Dado que o usuário está autenticado
  Quando GET /auth/me
  Então status 200 com nome, e-mail, avatar, tenant ativo, scopes
  E passwordHash e mfaSecret NÃO estão presentes na resposta

Cenário: Edição de perfil
  Dado que o usuário está autenticado
  Quando PATCH /auth/me com {name: "Novo Nome"}
  Então status 200 com dados atualizados

Cenário: Sem autenticação
  Dado que o usuário NÃO está autenticado
  Quando GET /auth/me
  Então status 401 (RFC 9457)
```

---

## FR-005 — Alteração de Senha Autenticada

- **Descrição:** O sistema DEVE permitir que o usuário autenticado altere sua própria senha.
- **Prioridade:** Must
- **Perfis impactados:** Todos os usuários autenticados
- **Done funcional:**
  - POST /auth/change-password aceita `{current_password, new_password}`
  - Valida senha atual via bcrypt compare
  - Atualiza passwordHash
  - Emite evento `auth.password_changed` (`tenant_id = request.session.tenantId` — pós-auth; FR-000-C07)
- **Efeito colateral?** [x] Sim — Idempotency-Key recomendado
- **Timeline/Notifications?** [x] Sim — evento `auth.password_changed` → DATA-003, SEC-002
- **Dependências:** FR-001, BR-002, DATA-003, SEC-000, SEC-002

```gherkin
Funcionalidade: Alteração de Senha Autenticada

Cenário: Alteração bem-sucedida
  Dado que o usuário está autenticado
  Quando POST /auth/change-password com senha atual correta e nova senha válida
  Então status 200
  E passwordHash atualizado no banco
  E evento auth.password_changed emitido

Cenário: Senha atual incorreta
  Dado que o usuário está autenticado
  Quando POST /auth/change-password com senha atual incorreta
  Então status 400 (RFC 9457) com detail "Senha atual incorreta"

Cenário: Nova senha não atende requisitos
  Dado que o usuário está autenticado
  Quando POST /auth/change-password com nova senha fraca
  Então status 422 (RFC 9457) com detalhes de validação
```

---

## FR-006 — Gestão de Usuários (CRUD + Soft Delete)

- **Descrição:** O sistema DEVE prover CRUD completo de usuários com auto-registro público, separação de tabelas (users + content_users), soft delete para LGPD e reenvio de convite de ativação.
- **Prioridade:** Must
- **Perfis impactados:** Usuário anônimo (registro), Administrador (gestão)
- **Done funcional:**
  - POST /api/v1/users — criação com campo `mode` obrigatório (enum: `invite`, `password`):
    - `mode='invite'`: gera token de ativação, envia e-mail, status=PENDING, `password` proibido (422 se fornecido)
    - `mode='password'`: define senha temporária, `force_pwd_reset=true` por padrão, status=PENDING
    - `role_id` opcional: quando fornecido, cria vinculação `tenant_users` atomicamente na mesma transação
    - Backward compatibility: auto-registro público (sem auth) infere `mode='password'`, `role_id` retorna 403 se fornecido sem auth
  - GET /api/v1/users — listagem paginada cursor-based (requer auth). Schema `UserListItem` inclui `role_id` e `role_name` (nullable — null se sem vinculação ao tenant ativo via header `X-Tenant-ID`). Backend faz LEFT JOIN com `tenant_users` + `roles`.
  - GET /api/v1/users/:id — detalhe (requer auth). Schema `UserDetail` inclui `invite_token_expired` (nullable boolean — null se nunca recebeu convite, true/false baseado em `invite_token_created_at + TTL > now()`. Não expõe o token — apenas booleano, LGPD/segurança).
  - PATCH /api/v1/users/:id — edição (requer users:user:write)
  - DELETE /api/v1/users/:id — soft delete (requer users:user:delete)
  - POST /api/v1/users/:id/invite/resend — reenvio de convite de ativação (operationId: `users_invite_resend`, requer `users:user:write`, apenas status=PENDING, gera novo token invalidando o anterior, atualiza `invite_token_created_at`, evento `user.invite_resent` com sensitivity_level=1, e-mail via Outbox Pattern, retorna 200 `{ message: "Invite sent" }` sem revelar e-mail/token, Idempotency-Key TTL 60s)
  - Inserção atômica em users + content_users (+ tenant_users se `role_id` fornecido) via transação Drizzle
  - E-mail de boas-vindas (mode=password) ou convite (mode=invite) assíncrono via Outbox
  - Validação condicional: `mode='invite' + password` → 422, `mode='password' - password` → 422, `role_id + role inexistente` → 422, `role_id + sem auth` → 403
  - POST /api/v1/users/:id/reset-password — admin inicia reset de senha para o usuário (operationId: `users_reset_password`, requer `users:user:write`, gera token de reset UUID e enfileira e-mail de reset via Outbox Pattern com dedupe_key `user_id + password_reset + timestamp`, evento `user.password_reset` com payload `{ user_id, reset_by, timestamp }`, retorna 200 `{ "message": "Password reset email sent." }`, erros: 401/403/404/500)
  - DELETE /api/v1/users/:id/invite — cancela convite pendente de ativação (operationId: `users_cancel_invite`, requer `users:user:delete`, pré-condição status MUST ser PENDING senão 409, altera status para INACTIVE e invalida token de convite, evento `user.invite_cancelled` com payload `{ user_id, cancelled_by, timestamp }`, retorna 200 `{ "data": { "id": "uuid", "status": "INACTIVE", "updatedAt": "ISO8601" } }`, erros: 401/403/404/409/500)
  - Eventos: `user.created`, `user.updated`, `user.deleted`, `user.invite_resent`, `user.password_reset`, `user.invite_cancelled`
- **Efeito colateral?** [x] Sim — Idempotency-Key em POST (TTL 60s)
- **Dependências:** BR-004, BR-009, BR-012, DATA-000, DATA-000-M01, DATA-003, INT-000, INT-000-M01, SEC-000, SEC-000-M01, SEC-002, UX-000

```gherkin
Funcionalidade: Gestão de Usuários

Cenário: Criação por convite com role
  Dado que o admin tem escopo users:user:write
  Quando POST /api/v1/users com mode='invite', email, full_name, role_id
  Então status 201 com usuário criado (status PENDING)
  E token de convite gerado
  E e-mail de convite enfileirado via Outbox
  E vinculação tenant_users criada com role_id na mesma transação

Cenário: Criação por senha temporária
  Dado que o admin tem escopo users:user:write
  Quando POST /api/v1/users com mode='password', email, full_name, password
  Então status 201 com usuário criado (status PENDING, force_pwd_reset=true)
  E e-mail de boas-vindas enfileirado

Cenário: Modo invite com password (erro)
  Quando POST /api/v1/users com mode='invite' e password preenchido
  Então status 422 com detail "Password must not be sent when mode is 'invite'"

Cenário: Auto-registro público (backward compatible)
  Quando POST /api/v1/users sem auth com email, password, full_name
  Então status 201 com usuário criado (status ACTIVE)
  E mode inferido como 'password' por compatibilidade

Cenário: Listagem inclui role do tenant ativo
  Dado que o admin está autenticado no tenant "filial-sp"
  E que o usuário "joao@empresa.com" tem role "Operador" nesse tenant
  Quando GET /api/v1/users
  Então cada item da lista inclui role_id e role_name
  E o usuário "joao@empresa.com" aparece com role_name "Operador"

Cenário: Usuário sem vinculação ao tenant
  Dado que o usuário "maria@empresa.com" não tem vinculação ao tenant ativo
  Quando GET /api/v1/users
  Então o item de "maria@empresa.com" tem role_id null e role_name null

Cenário: Token de convite válido
  Dado que o usuário tem status PENDING
  E que o token de convite foi gerado há menos de 48h (TTL padrão)
  Quando GET /api/v1/users/:id
  Então invite_token_expired = false

Cenário: Token de convite expirado
  Dado que o usuário tem status PENDING
  E que o token de convite foi gerado há mais de 48h
  Quando GET /api/v1/users/:id
  Então invite_token_expired = true

Cenário: Soft Delete
  Dado que admin tem escopo users:user:delete
  Quando DELETE /api/v1/users/:id
  Então deletedAt preenchido, status INACTIVE
  E evento user.deleted emitido

Cenário: Reenvio de convite bem-sucedido
  Dado que admin tem escopo users:user:write
  E que o usuário alvo tem status PENDING
  Quando POST /api/v1/users/:id/invite/resend com Idempotency-Key
  Então status 200 com { message: "Invite sent" }
  E token anterior invalidado, novo token gerado
  E invite_token_created_at atualizado para now()
  E evento user.invite_resent emitido via Outbox

Cenário: Reenvio para usuário não-PENDING
  Dado que o usuário alvo tem status ACTIVE
  Quando POST /api/v1/users/:id/invite/resend
  Então status 422 (RFC 9457) com detail explicando restrição de status

Cenário: Admin reseta senha de outro usuário
  Dado que o admin com scope "users:user:write" chama POST /users/:id/reset-password
  E o usuário existe com status ACTIVE
  Então resposta 200 com message "Password reset email sent."
  E domain event "user.password_reset" é emitido
  E e-mail de reset é enfileirado via Outbox

Cenário: Admin cancela convite pendente
  Dado que o admin com scope "users:user:delete" chama DELETE /users/:id/invite
  E o usuário existe com status PENDING
  Então resposta 200 com status alterado para INACTIVE
  E domain event "user.invite_cancelled" é emitido

Cenário: Tentativa de cancelar convite de usuário não-PENDING
  Dado que o usuário existe com status ACTIVE
  Quando admin chama DELETE /users/:id/invite
  Então resposta 409 ProblemDetails "User is not in PENDING status"
```

---

## FR-007 — Gestão de Roles/RBAC

- **Descrição:** O sistema DEVE prover CRUD de Roles com escopos de permissão, cache Redis e soft delete.
- **Prioridade:** Must
- **Perfis impactados:** Administrador
- **Done funcional:**
  - POST /api/v1/roles — criar role com escopos (requer users:role:write)
  - GET /api/v1/roles — listar roles (requer users:role:read)
  - PUT /api/v1/roles/:id — substituição completa de escopos (requer users:role:write)
  - DELETE /api/v1/roles/:id — soft delete (requer users:role:write)
  - Cache Redis `auth:scopes:role:{id}` com invalidação automática
  - Eventos: `role.created`, `role.updated`, `role.deleted` (`tenant_id = request.session.tenantId` — pós-auth; FR-000-C07)
- **Efeito colateral?** [x] Sim
- **Dependências:** BR-005, BR-006, BR-011, DATA-000, SEC-000

```gherkin
Funcionalidade: Gestão de Roles

Cenário: Criação de role
  Dado que o usuário tem escopo users:role:write
  Quando POST /api/v1/roles com name, scopes
  Então status 201 com role criada
  E evento role.created emitido

Cenário: Sem permissão
  Dado que o usuário NÃO tem escopo users:role:write
  Quando qualquer mutação em /roles
  Então status 403 (RFC 9457)
```

---

## FR-008 — Gestão de Filiais Multi-Tenant

- **Descrição:** O sistema DEVE prover CRUD de filiais (tenants) com soft delete e bloqueio administrativo.
- **Prioridade:** Must
- **Perfis impactados:** Administrador
- **Done funcional:**
  - POST /api/v1/tenants — criar filial (requer tenants:branch:write)
  - GET /api/v1/tenants — listar filiais do contexto
  - PATCH /api/v1/tenants/:id — editar (requer tenants:branch:write)
  - DELETE /api/v1/tenants/:id — soft delete (requer tenants:branch:write)
  - Bloqueio de filial: status BLOCKED impede operações
  - Eventos: `tenant.created`, `tenant.updated`, `tenant.status_changed`
- **Efeito colateral?** [x] Sim
- **Timeline/Notifications?** [x] Sim — eventos tenant.* → DATA-003, SEC-002
- **Dependências:** BR-004, BR-007, BR-009, DATA-000, DATA-003, SEC-000, SEC-002

```gherkin
Funcionalidade: Gestão de Filiais Multi-Tenant

Cenário: Criação de filial
  Dado que o usuário tem escopo tenants:branch:write
  Quando POST /api/v1/tenants com dados válidos
  Então status 201 com filial criada
  E evento tenant.created emitido

Cenário: Bloqueio de filial
  Dado que o admin tem escopo tenants:branch:write
  Quando PATCH /api/v1/tenants/:id com status BLOCKED
  Então filial bloqueada
  E evento tenant.status_changed emitido
  E operações nessa filial retornam 403

Cenário: Soft delete
  Dado que o admin tem escopo tenants:branch:write
  Quando DELETE /api/v1/tenants/:id
  Então deletedAt preenchido
  E evento tenant.updated emitido
```

---

## FR-009 — Vinculação Usuário-Filial com Role

- **Descrição:** O sistema DEVE permitir vincular usuários a filiais atribuindo roles específicas por filial. A vinculação pode ocorrer via endpoint dedicado OU atomicamente na criação do usuário (via `role_id` no `CreateUserRequest` — FR-006, FR-000-M01).
- **Prioridade:** Must
- **Perfis impactados:** Administrador
- **Done funcional:**
  - POST /api/v1/tenants/:tenantId/users — vincular usuário com role
  - POST /api/v1/users com `role_id` — vinculação atômica na criação (FR-006, FR-000-M01). Cria `tenant_users` na mesma transação Drizzle.
  - GET /api/v1/tenants/:tenantId/users — listar usuários da filial
  - PATCH /api/v1/tenants/:tenantId/users/:userId — alterar role
  - DELETE /api/v1/tenants/:tenantId/users/:userId — desvincular
  - Eventos: `tenant_user.added`, `tenant_user.role_changed`, `tenant_user.blocked`, `tenant_user.removed`
- **Efeito colateral?** [x] Sim
- **Timeline/Notifications?** [x] Sim — eventos tenant_user.* → DATA-003, SEC-002
- **Dependências:** FR-006, FR-007, FR-008, DATA-000, DATA-003, SEC-000, SEC-002

```gherkin
Funcionalidade: Vinculação Usuário-Filial

Cenário: Vincular usuário a filial
  Dado que o admin tem escopo tenants:branch:write
  Quando POST /api/v1/tenants/:tenantId/users com userId e roleId
  Então status 201 com vínculo criado
  E evento tenant_user.added emitido

Cenário: Alterar role do vínculo
  Dado que o admin tem escopo tenants:branch:write
  Quando PATCH /api/v1/tenants/:tenantId/users/:userId com novo roleId
  Então status 200 com role atualizada
  E cache Redis de scopes invalidado
  E evento tenant_user.role_changed emitido

Cenário: Desvincular usuário
  Dado que o admin tem escopo tenants:branch:write
  Quando DELETE /api/v1/tenants/:tenantId/users/:userId
  Então status 204
  E evento tenant_user.removed emitido
```

---

## FR-010 — Catálogo de Permissões

- **Descrição:** O sistema DEVE prover CRUD de escopos de permissão pré-definidos, servindo como catálogo canônico consumido pelo RBAC.
- **Prioridade:** Must
- **Perfis impactados:** Administrador
- **Done funcional:**
  - Escopos fundacionais pré-cadastrados (DOC-FND-000 §2.2)
  - CRUD para novos escopos (módulos futuros registram via PR)
  - Validação de formato regex `^[a-z][a-z0-9_]*(:[a-z][a-z0-9_]*){1,2}$` no cadastro
  - Eventos: `scope.created`, `scope.updated`, `scope.deleted`
- **Efeito colateral?** [x] Sim
- **Timeline/Notifications?** [x] Sim — eventos scope.* → DATA-003, SEC-002
- **Dependências:** BR-005, FR-007, DATA-003, SEC-000, SEC-002

```gherkin
Funcionalidade: Catálogo de Permissões

Cenário: Listagem de escopos
  Dado que o usuário tem escopo users:role:read
  Quando GET /api/v1/scopes
  Então status 200 com lista paginada de escopos

Cenário: Criação de escopo com formato válido
  Dado que o admin tem escopo users:role:write
  Quando POST /api/v1/scopes com {key: "modulo:entidade:acao", description: "..."}
  Então status 201 com escopo criado
  E evento scope.created emitido

Cenário: Criação com formato inválido
  Quando POST /api/v1/scopes com {key: "INVALIDO"}
  Então status 422 (RFC 9457) com detalhes de validação regex
```

---

## FR-011 — Endpoint GET /info

- **Descrição:** O sistema DEVE prover endpoint público para retorno de versão e metadados do sistema.
- **Prioridade:** Should
- **Perfis impactados:** Operações, Desenvolvedores
- **Done funcional:**
  - GET /info retorna versão da API, ambiente, timestamp
  - Sem autenticação necessária
- **Efeito colateral?** [ ] Não
- **Timeline/Notifications?** [ ] Não
- **Dependências:** Nenhuma

```gherkin
Funcionalidade: Endpoint de Informações do Sistema

Cenário: Consulta pública
  Quando GET /info sem autenticação
  Então status 200 com {version, environment, timestamp}

Cenário: Campos obrigatórios presentes
  Quando GET /info
  Então resposta contém campo "version" no formato semver
  E resposta contém campo "environment" (development|staging|production)
  E resposta contém campo "timestamp" em ISO 8601
```

---

## FR-012 — Telemetria UI (UIActionEnvelope)

- **Descrição:** O sistema DEVE prover pacote utilitário `ui-telemetry` para rastreio padronizado de ações da UI via contrato UIActionEnvelope (DOC-ARC-003 §2).
- **Prioridade:** Must
- **Perfis impactados:** Desenvolvedores frontend
- **Done funcional:**
  - Pacote instanciável com contrato UIActionEnvelope
  - Campos: screen_id, action, entity_type, tenant_id, operation_id, correlation_id, status, duration_ms
  - Integração com endpoint de ingestão de telemetria
  - POST /api/v1/telemetry/actions — ingestão de eventos UIActionEnvelope
- **Efeito colateral?** [ ] Não (leitura de telemetria é observacional)
- **Timeline/Notifications?** [ ] Não
- **Dependências:** NFR-000, DOC-ARC-003

```gherkin
Funcionalidade: Telemetria UI

Cenário: Envio de evento de telemetria
  Dado que o usuário está autenticado
  Quando POST /api/v1/telemetry/actions com UIActionEnvelope válido
  Então status 202 (accepted)
  E evento armazenado para processamento assíncrono

Cenário: Campos obrigatórios ausentes
  Quando POST /api/v1/telemetry/actions sem screen_id
  Então status 422 com detalhes de validação
```

---

## FR-013 — Middlewares de Correlação E2E

- **Descrição:** O sistema DEVE propagar `X-Correlation-ID` em toda requisição (inbound e outbound) para rastreabilidade ponta a ponta.
- **Prioridade:** Must
- **Perfis impactados:** Operações, Desenvolvedores
- **Done funcional:**
  - Middleware gera ou aceita X-Correlation-ID (UUID v4)
  - Se header presente na request: reutiliza; se ausente: gera novo
  - ID propagado em logs, respostas HTTP, eventos de domínio, integrações outbound
  - Erros RFC 9457 incluem `extensions.correlationId`
  - Response header `X-Correlation-ID` sempre presente
- **Efeito colateral?** [ ] Não
- **Timeline/Notifications?** [ ] Não
- **Dependências:** NFR-000

```gherkin
Funcionalidade: Correlação E2E

Cenário: Request sem correlation ID
  Quando qualquer request HTTP sem header X-Correlation-ID
  Então middleware gera UUID v4 automaticamente
  E response contém header X-Correlation-ID com o UUID gerado
  E logs incluem o correlation_id

Cenário: Request com correlation ID
  Quando request HTTP com header X-Correlation-ID: "abc-123"
  Então middleware reutiliza "abc-123"
  E response contém header X-Correlation-ID: "abc-123"

Cenário: Propagação em erro
  Quando request resulta em erro
  Então body RFC 9457 contém extensions.correlationId
```

---

## FR-014 — Storage e Upload Centralizado

- **Descrição:** O sistema DEVE prover módulo de storage com presigned URLs para upload seguro de arquivos.
- **Prioridade:** Must
- **Perfis impactados:** Todos os usuários
- **Done funcional:**
  - POST /api/v1/storage/presigned-url — gera presigned URL para upload direto
  - Entity types fundacionais: `user` (avatar, attachment), `tenant` (attachment) — DOC-FND-000 §6
  - Validação de tipo MIME e tamanho máximo por entity_type
  - Confirmação de upload bem-sucedido via callback/webhook
  - Eventos: `storage.upload_completed`, `storage.file_deleted`
- **Efeito colateral?** [x] Sim
- **Timeline/Notifications?** [x] Sim — eventos storage.* → DATA-003, SEC-002
- **Dependências:** SEC-000, SEC-002, DATA-000, DATA-003

```gherkin
Funcionalidade: Storage e Upload Centralizado

Cenário: Geração de presigned URL
  Dado que o usuário está autenticado
  Quando POST /api/v1/storage/presigned-url com {entity_type: "user", purpose: "avatar", content_type: "image/png"}
  Então status 200 com presigned URL válida (TTL configurável)

Cenário: Tipo MIME não permitido
  Quando POST /api/v1/storage/presigned-url com content_type não permitido para o entity_type
  Então status 422 com detalhes de validação

Cenário: Tamanho excede limite
  Quando upload para presigned URL excede tamanho máximo
  Então upload rejeitado pelo storage provider
```

---

## FR-015 — MFA / TOTP (RFC 6238)

- **Descrição:** O sistema DEVE prover fluxo completo de MFA via TOTP (RFC 6238): setup com geração de secret/QR, verificação de código e desativação.
- **Prioridade:** Must
- **Perfis impactados:** Todos os usuários autenticados
- **Done funcional:**
  - POST /auth/mfa/setup — gera `mfa_secret` + URI otpauth para QR code (requer sessão ativa)
  - POST /auth/mfa/verify — verifica código TOTP de 6 dígitos contra o secret
  - DELETE /auth/mfa — desativa MFA (requer confirmação de senha)
  - Se MFA ativo no login: emite `temp_token` escopo `mfa-only` (TTL 5min — BR-008)
  - Eventos: `auth.mfa_enabled`, `auth.mfa_verified`, `auth.mfa_failed`
- **Efeito colateral?** [x] Sim — Idempotency-Key recomendado em setup
- **Dependências:** BR-008, FR-001, DATA-000, DATA-003, SEC-000

```gherkin
Funcionalidade: MFA / TOTP

Cenário: Setup de MFA
  Dado que o usuário está autenticado e NÃO tem MFA ativo
  Quando POST /auth/mfa/setup
  Então status 200 com secret e otpauth URI
  E evento auth.mfa_enabled NÃO é emitido ainda

Cenário: Verificação MFA bem-sucedida no login
  Dado que o usuário recebeu temp_token após login
  Quando POST /auth/mfa/verify com código TOTP válido
  Então status 200 com access_token e refresh_token
  E sessão criada em user_sessions
  E evento auth.mfa_verified emitido

Cenário: Código MFA incorreto
  Dado que o usuário tem temp_token válido
  Quando POST /auth/mfa/verify com código inválido
  Então status 401
  E evento auth.mfa_failed emitido
```

---

## FR-016 — SSO OAuth2 (Google + Microsoft)

- **Descrição:** O sistema DEVE prover login via SSO OAuth2 com Google e Microsoft/Azure AD usando Authorization Code Flow. Quando o e-mail do provider já existe com conta nativa, o sistema DEVE exigir confirmação via senha nativa antes de vincular o provider SSO (ADR-004).
- **Prioridade:** Must
- **Perfis impactados:** Todos os usuários
- **Done funcional:**
  - GET /auth/google — redirect para Google OAuth2
  - GET /auth/google/callback?code=... — troca code por token, cria/reutiliza usuário, emite JWT
  - GET /auth/microsoft — redirect para Microsoft/Azure AD
  - GET /auth/microsoft/callback?code=... — troca code por token, cria/reutiliza usuário
  - Redirect final: `{FRONTEND_URL}/sso-success?token=JWT&provider={provider}&is_new_user={bool}`
  - **Fluxo de Identity Linking (ADR-004):** Se e-mail do provider já existe com conta nativa:
    1. Callback SSO detecta e-mail existente sem `sso_provider_id` vinculado
    2. Gera `temp_sso_token` (escopo `sso-link-only`, TTL 10min) com claims: `email`, `provider`, `provider_id`
    3. Redirect para `{FRONTEND_URL}/sso-link-confirm?token={temp_sso_token}&provider={provider}`
    4. Frontend exibe tela de confirmação solicitando senha nativa
    5. POST /auth/sso/link-confirm aceita `{temp_sso_token, password}`:
       - Valida `temp_sso_token` (escopo, TTL, integridade)
       - bcrypt compare da senha nativa
       - Vincula `sso_provider` + `sso_provider_id` à conta existente
       - Cria sessão em `user_sessions`
       - Emite evento `auth.sso_linked` (sensitivity_level=1)
       - Retorna `{access_token, refresh_token, token_type, expires_in, user}`
    6. Se senha incorreta: retorna 400 (RFC 9457) — máx. 5 tentativas, depois invalida `temp_sso_token`
  - Se Microsoft não retorna `mail`: redirect `/login?error=no_email_provided&provider=microsoft`
  - Eventos: `auth.sso_login`, `auth.sso_linked`
- **Efeito colateral?** [x] Sim (criação de usuário quando novo, vinculação de provider)
- **Timeline/Notifications?** [x] Sim — eventos auth.sso_login, auth.sso_linked → DATA-003, SEC-002
- **Dependências:** INT-001, INT-002, FR-001, DATA-000, DATA-003, SEC-000, SEC-002, ADR-004

```gherkin
Funcionalidade: SSO OAuth2

Cenário: Login Google — usuário novo
  Quando GET /auth/google/callback com code válido
  E o e-mail não existe no sistema
  Então cria usuário + content_users em transação
  E emite evento auth.sso_login
  E redirect para /sso-success com is_new_user=true

Cenário: Login Google — usuário existente com SSO já vinculado
  Quando GET /auth/google/callback com code válido
  E o e-mail já existe no sistema com sso_provider_id vinculado
  Então reutiliza registro existente
  E emite evento auth.sso_login
  E redirect para /sso-success com is_new_user=false

Cenário: Login SSO — e-mail existente com conta nativa (Identity Linking)
  Dado que o e-mail do provider SSO já existe com conta nativa sem vínculo SSO
  Quando GET /auth/google/callback com code válido
  Então gera temp_sso_token com escopo sso-link-only (TTL 10min)
  E redirect para /sso-link-confirm com token e provider

Cenário: Confirmação de Identity Linking — senha correta
  Dado que o usuário recebeu temp_sso_token após SSO callback
  Quando POST /auth/sso/link-confirm com temp_sso_token e senha nativa correta
  Então status 200 com access_token, refresh_token, user
  E sso_provider e sso_provider_id vinculados à conta existente
  E evento auth.sso_linked emitido
  E sessão criada em user_sessions

Cenário: Confirmação de Identity Linking — senha incorreta
  Dado que o usuário recebeu temp_sso_token
  Quando POST /auth/sso/link-confirm com senha incorreta
  Então status 400 (RFC 9457) com detail "Senha incorreta"
  E tentativas restantes decrementadas

Cenário: Identity Linking — tentativas esgotadas
  Dado que o usuário errou a senha 5 vezes
  Quando POST /auth/sso/link-confirm
  Então status 429 com detail "Token invalidado por excesso de tentativas"
  E temp_sso_token é invalidado

Cenário: Falha no SSO
  Quando GET /auth/google/callback e o token exchange falha
  Então redirect para /login?error=sso_failed&provider=google
```

---

## FR-017 — Recuperação de Senha (Forgot / Reset)

- **Descrição:** O sistema DEVE prover fluxo completo de recuperação de senha via token UUID com TTL de 1 hora.
- **Prioridade:** Must
- **Perfis impactados:** Todos os usuários (público)
- **Done funcional:**
  - POST /auth/forgot-password aceita `{email}` — gera token UUID, TTL 1h, envia e-mail
  - POST /auth/reset-password aceita `{token, new_password}` — valida token, redefine senha
  - Resposta de forgot-password DEVE ser sempre genérica (BR-001 — User Enumeration Prevention)
  - Token expirado → 422 com mensagem "Link expirado"
  - Eventos: `auth.forgot_password_requested`, `auth.password_reset`
- **Efeito colateral?** [x] Sim — Idempotency-Key recomendado
- **Dependências:** BR-001, INT-005, FR-001, DATA-000, DATA-003, SEC-000

```gherkin
Funcionalidade: Recuperação de Senha

Cenário: Solicitação com e-mail existente
  Dado que o e-mail está cadastrado
  Quando POST /auth/forgot-password
  Então status 200 com mensagem genérica
  E e-mail de reset enviado com token UUID
  E evento auth.forgot_password_requested emitido

Cenário: Solicitação com e-mail inexistente
  Dado que o e-mail NÃO está cadastrado
  Quando POST /auth/forgot-password
  Então status 200 com mensagem genérica idêntica (BR-001)
  E NENHUM e-mail é enviado

Cenário: Reset com token válido
  Dado que o token é válido e não expirou
  Quando POST /auth/reset-password com nova senha
  Então senha atualizada com sucesso
  E evento auth.password_reset emitido

Cenário: Reset com token expirado
  Dado que o token tem mais de 1 hora
  Quando POST /auth/reset-password
  Então status 422 com mensagem "Link expirado"
```

---

## FR-018 — Sign in with Apple (OIDC/JWKS)

- **Descrição:** O sistema DEVE prover login via Sign in with Apple usando Authorization Code Flow com OIDC/JWKS.
- **Prioridade:** Should
- **Perfis impactados:** Todos os usuários
- **Done funcional:**
  - GET /auth/apple — redirect para Apple Identity Service
  - POST /auth/apple/callback — recebe form_post com `{code, id_token}`
  - Validação de id_token via JWKS (`appleid.apple.com/auth/keys`, cache TTL 24h)
  - Userinfo extraído do id_token (sub, email, name — nome apenas no primeiro login)
  - Se usuário já existe (por email ou Apple sub): reutiliza registro
  - Evento: `auth.sso_login`
- **Efeito colateral?** [x] Sim (criação de usuário quando novo)
- **Dependências:** INT-003, FR-001, FR-016, DATA-000, DATA-003, SEC-000

```gherkin
Funcionalidade: Sign in with Apple

Cenário: Login Apple — usuário novo
  Quando POST /auth/apple/callback com code e id_token válidos
  E o e-mail/sub não existe no sistema
  Então cria usuário + content_users
  E emite evento auth.sso_login
  E redirect para /sso-success com provider=apple

Cenário: Login Apple — nome apenas no primeiro login
  Quando POST /auth/apple/callback pela primeira vez
  Então o nome é extraído do id_token e salvo
  Quando POST /auth/apple/callback em logins subsequentes
  Então o nome NÃO é atualizado (Apple só envia na primeira vez)
```

---

## FR-019 — Motor de Gates de Pipeline CI

- **Descrição:** O sistema DEVE prover motor de validação de gates CI que valida screen manifests, schemas e contratos OpenAPI antes do deploy.
- **Prioridade:** Must
- **Perfis impactados:** Desenvolvedores, CI/CD
- **Done funcional:**
  - Validador de Screen Manifests (YAML schema v1)
  - Validação de contratos OpenAPI contra normas DOC-ARC-001
  - Validação de schemas Drizzle contra DATA-xxx
  - Gate binário: passa (exit 0) ou falha (exit 1) com relatório estruturado
  - Integração com pipeline CI (GitHub Actions / GitLab CI)
  - Relatório de erros em formato JSON (parseable por CI)
- **Efeito colateral?** [ ] Não (read-only, valida artefatos)
- **Timeline/Notifications?** [ ] Não
- **Dependências:** NFR-000

```gherkin
Funcionalidade: Motor de Gates CI

Cenário: Todos os gates passam
  Dado que todos os artefatos estão conformes
  Quando o motor de gates executa
  Então exit code 0
  E relatório indica 0 erros

Cenário: Screen manifest inválido
  Dado que um screen manifest tem campo obrigatório ausente
  Quando o motor de gates executa
  Então exit code 1
  E relatório indica o arquivo e o campo faltante

Cenário: Scope não registrado no catálogo
  Dado que um screen manifest referencia scope "modulo:acao" não existente em DOC-FND-000 §2.2
  Quando o motor de gates executa
  Então exit code 1
  E relatório indica scope desconhecido com sugestão de registro
```

---

## FR-020 — Entry Point Registration (DOC-ARC-004)

- **Descrição:** O entry point `apps/api/src/index.ts` DEVE registrar todos os route plugins dos módulos gerados via `app.register(plugin, { prefix })`. Rotas inline são PROIBIDAS.
- **Prioridade:** Must
- **Done funcional:**
  - Entry point importa e registra route plugins de todos os módulos (MOD-000 a MOD-010)
  - Auth plugin (`verifySession`, `requireScope`) registrado como Fastify decorators
  - DI plugin (`dipiContainer`) registrado como request decorator
  - `foundationErrorHandler` registrado como global error handler
  - `app.printRoutes()` no startup para validação visual
  - Nenhuma rota inline — todo SQL/bcrypt/schema removido do entry point
  - Script `scripts/validate-route-registration.ts` confirma zero gaps
- **Dependências:** DOC-ARC-004, DOC-GNP-00

> **Ref:** FR-000-C03. O entry point original continha 3 rotas hardcoded (~190 linhas SQL inline). FR-000-C02 adicionou `/auth/me` inline como hotfix. FR-000-C03 reescreve completamente para wiring modular.

---

## FR-021 — JWT DEVE conter tenant e scopes (FR-000-C05)

- **Descrição:** O `LoginUseCase` DEVE resolver o tenant ativo e os scopes da role antes de gerar o JWT. O JWT DEVE conter claims `tid` (tenantId) e `scopes` (string[]).
- **Prioridade:** Must
- **Done funcional:**
  - `LoginUseCase` busca `tenant_users` ativo via `TenantUserRepository.findByUserId()`
  - Busca scopes da role via `RoleRepository.findById()`
  - Inclui `tenantId` e `scopes` no `tokenService.generatePair()`
  - Sem tenant ativo → 401 "User has no active tenant association"
  - Role sem scopes → JWT com `scopes: []` (válido)
  - Novas dependências no construtor: `TenantUserRepository`, `RoleRepository`
- **Dependências:** FR-001, BR-003, SEC-000

> **Ref:** FR-000-C05. spec-fix-auth-flow-session-expired Fase 2.

---

## FR-022 — forceLogout para sessão expirada (FR-000-C06)

- **Descrição:** Quando o frontend detecta 401 em `GET /auth/me`, DEVE executar `forceLogout()` que limpa localStorage + cache React Query + full reload para `/login`. Navigate() é PROIBIDO (causa redirect loop).
- **Prioridade:** Must
- **Done funcional:**
  - Nova utility `forceLogout(queryClient)` em `foundation/utils/force-logout.ts`
  - AppShell usa `forceLogout` ao detectar 401 (não `navigate('/login')`)
  - Toast com `id: 'session-expired'` para deduplicação Sonner
  - `useAuthMe` desabilita retry em 401 (`retry: false` para status 401)
  - Full reload via `window.location.href` para resetar router context estático
- **Dependências:** FR-001, FR-004, UX-001

> **Ref:** FR-000-C06. spec-fix-session-timeout-redirect-loop.

---

- **estado_item:** READY
- **owner:** arquitetura
- **data_ultima_revisao:** 2026-03-30
- **rastreia_para:** US-MOD-000, US-MOD-000-F01, US-MOD-000-F02, US-MOD-000-F03, US-MOD-000-F04, US-MOD-000-F05, US-MOD-000-F06, US-MOD-000-F07, US-MOD-000-F08, US-MOD-000-F09, US-MOD-000-F10, US-MOD-000-F11, US-MOD-000-F12, US-MOD-000-F13, US-MOD-000-F14, US-MOD-000-F15, US-MOD-000-F16, US-MOD-000-F17, BR-000, DATA-000, DATA-003, SEC-000, SEC-002, INT-000, DOC-FND-000
- **referencias_exemplos:** DOC-ESC-001 §3 (padrões MUST), DOC-FND-000 §1-§6, DOC-ARC-003 §2 (UIActionEnvelope), DOC-FND-000 §2.1-§2.2 (scopes canônicos 3 segmentos)
- **evidencias:** Extraído de US-MOD-000-F01 a F17. FR-015/016/017/018 adicionados. FR-019 (Gates CI) adicionado para F15. Gherkin completado em FR-004/005/008/009/010/011/012/013/014. Flags timeline_or_notifications e dependências DATA-003/SEC-002 corrigidas

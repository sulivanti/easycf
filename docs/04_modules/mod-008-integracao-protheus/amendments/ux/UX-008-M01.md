> ⚠️ **ARQUIVO GERIDO POR AUTOMAÇÃO.**
> - Emenda sobre documento base em estado READY.
> - Para novas emendas, use a skill `create-amendment`.

# Emenda: UX-008-M01

- **Documento base:** [UX-008](../../requirements/ux/UX-008.md)
- **estado_item:** MERGED
- **status_implementacao:** PENDENTE
- **Natureza:** M (Melhoria)
- **Data:** 2026-03-31
- **owner:** arquitetura
- **Motivação:** Alinhar componentes React de /integracoes/rotinas (UX-INTEG-001) e /integracoes/monitor (UX-INTEG-002) com designs validados no Penpot (frames 50-IntegrationEditor e 50-IntegrationMonitor). Gaps identificados: Editor — lista sem busca/header, content header ausente, readonly banner com cores incorretas, item ativo sem estilo spec. Monitor — falta card Latência Média + progress bar, AutoRefreshIndicator, filtros completos (rotina/serviço/período), JSON viewer, seções colapsáveis no detail panel, footer com contagem.
- **rastreia_para:** docs/03_especificacoes/ux/50-integrations-spec.md, docs/03_especificacoes/ux/50-integrations-ref.html, Penpot frame 50-IntegrationEditor, Penpot frame 50-IntegrationMonitor, ux-integ-001.editor-rotinas-integ.yaml, ux-integ-002.monitor-integracoes.yaml

---

## Detalhamento

### D1 — Layout RoutineEditorPage (atualizado)

O `RoutineEditorPage` DEVE implementar split-view com content header dedicado:

```
RoutineEditorPage (flex h-full)
├── RoutineList (w:280px, bg:#FFF, border-right 1px #E8E8E6, overflow-y auto)
│   ├── ListHeader (h:48px, p:0 16px, flex align-center justify-between, border-bottom 1px #E8E8E6)
│   │   ├── "Rotinas" (13px 700 #111111)
│   │   └── BtnNova (24×24, r:6, border 1px #E8E8E6, ícone "+" 16px #2E86C1)
│   ├── SearchInput (m:12px, h:36, r:6, border #E8E8E6, p:0 12px 0 32px)
│   │   ├── ÍconeLupa (14×14, stroke #CCCCCC, posição left 10px)
│   │   └── placeholder "Buscar rotina..." (12px 400 #CCCCCC)
│   └── RoutineItems (lista filtrada pela busca)
│       ├── RoutineItem (p:12px 16px, border-bottom 1px #F0F0EE, cursor pointer, hover bg:#F8F8F6)
│       │   ├── Row (flex justify-between align-center)
│       │   │   ├── Nome "CTB-001" (13px 600 #111111, truncate)
│       │   │   └── StatusBadge "DRAFT" (10px 700 uppercase, cores amber)
│       │   └── Sub "Protheus HML → /WSRESTPV001" (11px 400 #888888, mt:2px, truncate)
│       └── RoutineItem.active (bg:#E3F2FD, border-left 3px #2E86C1)
│           └── Nome em #2E86C1 (bold)
│
├── EditorPane (flex:1, flex-col, overflow hidden)
│   ├── ContentHeader (h:64px, bg:#FFF, border-bottom 1px #E8E8E6, p:0 24px, flex align-center justify-between)
│   │   ├── Esquerda (flex align-center gap:12px)
│   │   │   ├── RoutineName "CTB-001 — Lançamento Contábil" (18px 700 #111111)
│   │   │   └── StatusBadge "DRAFT" (amber) ou "PUBLISHED" (verde)
│   │   └── Direita (flex gap:8px)
│   │       ├── BtnTest (h:36, r:8, border 1px #E8E8E6, bg:#FFF)
│   │       │   └── "Testar HML" (13px 600 #555555)
│   │       ├── BtnPublish (h:36, r:8, bg:#2E86C1) — apenas DRAFT
│   │       │   └── "Publicar" (13px 700 #FFFFFF)
│   │       └── BtnFork (h:36, r:8, border 1px #2E86C1, bg:transparent) — apenas PUBLISHED
│   │           └── "Nova Versão" (13px 600 #2E86C1)
│   │
│   ├── [ReadonlyBanner] (bg:#E3F2FD, border 1px #90CAF9, r:8, p:12px 20px, m:0 20px)
│   │   └── "Rotina publicada — use 'Nova versão' para editar." (13px 500 #2E86C1)
│   │
│   ├── TabBar (h:44px, bg:#FFF, border-bottom 1px #E8E8E6, p:0 20px, flex gap:24px)
│   │   ├── Tab.active (13px 700 #2E86C1, border-bottom 2px #2E86C1)
│   │   └── Tab.inactive (13px 500 #888888, border-bottom transparent)
│   │
│   └── TabContent (flex:1, overflow-y auto, p:20px, bg:#F5F5F3)
│       └── {conteúdo da aba ativa}
```

Mudanças em relação ao código atual:
- **NOVO:** `ListHeader` com título "Rotinas" e botão "+"
- **NOVO:** `SearchInput` com ícone lupa e filtro local
- **UPDATE:** Item ativo: `bg-[#E3F2FD] border-l-[3px] border-l-[#2E86C1]` (antes: `bg-accent`)
- **UPDATE:** Nome da rotina ativa em `text-[#2E86C1]` (antes: cor padrão)
- **NOVO:** `ContentHeader` (64px) com nome da rotina + status + botões de ação
- **UPDATE:** `ReadonlyBanner` cores: `bg-[#E3F2FD] border-[#90CAF9] text-[#2E86C1]` (antes: amber)
- **UPDATE:** TabBar com cores exatas: ativa `#2E86C1`, inativa `#888888`
- **MOVER:** Botões Publicar/Testar/Fork do corpo do editor para o `ContentHeader`

### D2 — MonitorHeader / MetricCards (atualizado)

O `MonitorHeader` DEVE exibir 4 cards (não 3) com progress bar e DLQ alert:

```
MetricCards (flex gap:16px, mt:20px)
├── Card "Taxa de Sucesso" (flex:1, bg:#FFF, r:12, border 1px #E8E8E6, p:20px)
│   ├── Label "Taxa de Sucesso" (12px 500 #888888)
│   ├── Value "98.5%" (28px 800 #1E7A42, mt:4px)
│   └── ProgressBar (w:100%, h:4, r:2, bg:#E8E8E6, mt:8px)
│       └── Fill (w:{rate}%, h:4, r:2, bg:#27AE60)
│
├── Card "Total (24h)" (flex:1, bg:#FFF, r:12, border 1px #E8E8E6, p:20px)
│   ├── Label "Total (24h)" (12px 500 #888888)
│   └── Value "{total}" (28px 800 #111111, mt:4px)
│
├── Card "Em DLQ" (flex:1, bg:#FFEBEE quando count>0, r:12, border 1px {#F5C6CB|#E8E8E6}, p:20px)
│   ├── Label "Em DLQ" (12px 500 #888888)
│   ├── Value "{dlq}" (28px 800 #C0392B se >0, mt:4px)
│   └── [DLQSub] "requer atenção" (10px 600 #C0392B) — apenas se dlq > 0
│
└── Card "Latência Média" (flex:1, bg:#FFF, r:12, border 1px #E8E8E6, p:20px)
    ├── Label "Latência Média" (12px 500 #888888)
    └── Value "{avg_latency}ms" (28px 800 #111111, mt:4px)
```

Mudanças:
- **NOVO:** Card "Latência Média" (4º card — requer campo `avg_latency_ms` em MetricsDTO)
- **NOVO:** ProgressBar no card "Taxa de Sucesso"
- **UPDATE:** Card DLQ: `bg-[#FFEBEE] border-[#F5C6CB]` quando count > 0 + sub "requer atenção"
- **UPDATE:** Grid `grid-cols-4` (antes: `grid-cols-3`)
- **UPDATE:** Radius `rounded-xl` (12px, antes: `rounded-lg` 8px)

### D3 — AutoRefreshIndicator (novo componente)

Criar `apps/web/src/modules/integration-protheus/components/AutoRefreshIndicator.tsx`:

```
AutoRefreshIndicator (inline-flex align-center gap:6px)
├── Dot (6×6, r:50%, bg:#2E86C1, animate pulse 2s infinite)
└── "Atualizando..." (12px 500 #2E86C1)
```

- Visível no `PageHeader` do Monitor à direita
- Ativo quando há logs com status RUNNING ou QUEUED
- Animate: opacity pulse 2s (0→1→0)

### D4 — Filtros completos do Monitor (atualizado)

O `FilterBar` do Monitor DEVE incluir todos os filtros da spec:

```
Filters (flex gap:12px, mt:20px, flex-wrap:wrap, align-center)
├── SelectRotina (w:200, h:36, r:6, border #E8E8E6, font 12px)
│   └── placeholder "Todas as rotinas"
├── SelectStatus (w:160, h:36, r:6)
│   └── options: Todos, QUEUED, RUNNING, SUCCESS, FAILED, DLQ
├── SelectServico (w:180, h:36, r:6)
│   └── placeholder "Todos os serviços"
├── InputCorrelation (w:240, h:36, r:6)
│   └── placeholder "Correlation ID"
├── DateRange (flex gap:8px)
│   ├── InputDe (w:140, h:36, r:6, type:date)
│   └── InputAte (w:140, h:36, r:6, type:date)
└── BtnLimpar (h:36, r:6, p:0 12px, border #E8E8E6, bg:#FFF)
    └── "Limpar" (12px 600 #555555)
```

Mudanças:
- **NOVO:** Select "Rotina" (requer hook `useIntegrationRoutines`)
- **NOVO:** Select "Serviço" (requer hook `useServicesList`)
- **NOVO:** DateRange com inputs de/até (query params `from`, `to`)
- **UPDATE:** Sincronizar todos os filtros com URL query params

### D5 — LogTable footer com contagem (atualizado)

A tabela de logs DEVE incluir footer com contagem e "Carregar mais":

```
TableFooter (h:52px, p:0 20px, flex justify-between align-center, border-top 1px #F0F0EE)
├── "Exibindo {visíveis} de {total} chamadas" (12px 400 #888888)
└── BtnCarregar "Carregar mais" (13px 600 #2E86C1, cursor pointer, sem borda) — visível se has_more
```

Mudanças:
- **UPDATE:** Mover "Carregar mais" para dentro do footer da tabela (antes: botão externo abaixo)
- **NOVO:** Contagem "Exibindo X de Y" (requer `total_count` no response da API ou no MetricsDTO)

### D6 — LogDetailPanel (atualizado)

O painel de detalhe DEVE ter:

```
DetailPanel (w:480px, bg:#FFF, border-left 1px #E8E8E6, shadow -4px 0 24px rgba(0,0,0,0.08), flex-col)
├── DetailHeader (h:56px, p:0 20px, flex align-center justify-between, border-bottom 1px #E8E8E6)
│   ├── "Detalhes da Chamada" (16px 700 #111111)
│   └── BtnFechar (X 18×18, stroke #888888, hover #333333)
│
├── DetailBody (flex:1, overflow-y auto, p:20px)
│   ├── CollapsibleSection "Resumo" (aberto por padrão)
│   │   ├── Row: "Rotina:" "{nome} v{version}" (13px)
│   │   ├── Row: "Serviço:" "{service_name}" (13px)
│   │   ├── Row: "Status:" StatusBadge
│   │   ├── Row: "Correlation ID:" "{id}" (monospace, copy icon)
│   │   └── Row: "Caso:" link "/casos/{id}" (13px 600 #2E86C1) — se case_id
│   │
│   ├── CollapsibleSection "Request" (aberto por padrão)
│   │   ├── MethodURL "POST /WSRESTPV001/MATA410" (13px 600 #333, bg:#F8F8F6, r:6, p:8px 12px)
│   │   ├── Headers (mt:8px)
│   │   │   ├── "Authorization: ***" (12px monospace #C0392B) + tooltip segurança
│   │   │   └── "Content-Type: application/json" (12px monospace #333333)
│   │   └── JSONViewer (mt:8px, bg:#F8F8F6, r:6, border 1px #E8E8E6, p:12px, max-h:200, overflow-y auto)
│   │
│   ├── CollapsibleSection "Response" (aberto por padrão)
│   │   ├── HttpStatus badge
│   │   └── JSONViewer (mesma estrutura)
│   │
│   ├── [CollapsibleSection "Erro"] (apenas FAILED/DLQ)
│   │   └── ErrorMessage (13px 400 #333, bg:#FFEBEE, r:6, p:12px)
│   │
│   └── CollapsibleSection "Histórico de Tentativas" (colapsado por padrão)
│       └── AttemptList
│           └── AttemptItem (flex gap:8px, p:8px 0, border-bottom 1px #F0F0EE)
│               ├── "Tentativa {n}" (12px 600 #333333)
│               ├── StatusBadge (mini)
│               ├── Date (12px 400 #888888)
│               └── Link "Ver" (12px 600 #2E86C1)
│
└── DetailFooter (h:56px, p:0 20px, border-top 1px #E8E8E6, flex align-center justify-end)
    └── [BtnReprocessar] (apenas DLQ: h:36, r:8, bg:#E74C3C, p:0 16px)
        └── "Reprocessar" (13px 700 #FFFFFF)
```

Mudanças:
- **UPDATE:** Largura: `w-[480px]` (antes: `w-96` = 384px)
- **NOVO:** `DetailHeader` com título "Detalhes da Chamada" + botão fechar X
- **NOVO:** `CollapsibleSection` — componente reutilizável com chevron toggle
- **NOVO:** `JSONViewer` — bloco formatado com syntax highlight básico
- **UPDATE:** Seções organizadas em colapsáveis (Resumo, Request, Response, Erro, Histórico)
- **NOVO:** `DetailFooter` com botão "Reprocessar" em vermelho (#E74C3C) para DLQ
- **UPDATE:** Botão "Reprocessar" estilo danger (antes: outline amber)
- **NOVO:** Masked headers com tooltip "Dado sensível mascarado por política de segurança."

### D7 — Componentes shared novos

#### 7.1 CollapsibleSection

Criar `apps/web/src/modules/integration-protheus/components/CollapsibleSection.tsx`:

```typescript
interface CollapsibleSectionProps {
  title: string;
  defaultOpen?: boolean;
  titleColor?: string; // default #111111
  children: React.ReactNode;
}
```

- Título com chevron toggle (12×12, stroke #888888, rotate 90° quando aberto)
- Animação: altura com transition 200ms
- Reutilizável no LogDetailPanel e futuras telas

#### 7.2 JSONViewer

Criar `apps/web/src/modules/integration-protheus/components/JSONViewer.tsx`:

```typescript
interface JSONViewerProps {
  data: unknown;
  maxHeight?: number; // default 200
}
```

- Fundo `bg-[#F8F8F6]`, `rounded-md`, border 1px `#E8E8E6`, p:12px
- `font-family: monospace`, `font-size: 12px`, `color: #333333`
- `max-height: {maxHeight}px`, `overflow-y: auto`
- `white-space: pre`, `line-height: 1.5`
- JSON.stringify com indentação de 2 espaços

### D8 — ForkModal textarea (atualizado)

O ForkModal DEVE usar `textarea` ao invés de `Input` para o campo "Motivo":

- `textarea` com `rows:3`, `w:100%`, `h:80px`, `r:8`, border `#E8E8E6`, p:`10px 14px`
- placeholder: "Descreva o motivo (mín. 10 caracteres)"
- Título: "Nova versão (fork)" (18px 700 #111111)
- Subtítulo: "Será criado um novo DRAFT com a configuração atual copiada." (13px 400 #555555)

### D9 — Estilo dos badges de status (atualizado)

Os badges de status DEVEM usar as cores exatas da spec:

```
DRAFT      text:#B8860B  bg:#FFF3E0  border:1px #FFE0B2
PUBLISHED  text:#1E7A42  bg:#E8F8EF  border:1px #B5E8C9
SUCCESS    text:#1E7A42  bg:#E8F8EF  border:1px #B5E8C9
FAILED     text:#C0392B  bg:#FFEBEE  border:1px #F5C6CB
DLQ        text:#FFFFFF  bg:#C0392B  border:1px #C0392B
QUEUED     text:#888888  bg:#F5F5F3  border:1px #E8E8E6
RUNNING    text:#2E86C1  bg:#E3F2FD  border:1px #90CAF9  animation:pulse 2s infinite
```

---

## Impacto nos Pilares

- **DATA:** Possível impacto — MetricsDTO precisa de campo `avg_latency_ms` (latência média) e `total_count` (total para paginação). Verificar se endpoint GET /admin/integration-logs/metrics já retorna esses campos.
- **FR:** Possível impacto — se `avg_latency_ms` não está no MetricsDTO, endpoint de métricas precisa de atualização. Filtros de rotina, serviço e período precisam de query params no endpoint de logs.
- **UX (manifests):** Atualizar manifests com novos componentes: AutoRefreshIndicator, CollapsibleSection, JSONViewer.
- **BR:** Sem impacto.
- **SEC:** Sem impacto (mascaramento já implementado no servidor).

---

## Amendments Derivados

| ID | Pilar | Módulo | Natureza | Estado |
|---|---|---|---|---|
| [FR-008-M01](../fr/FR-008-M01.md) | FR | mod-008 | M | MERGED |

---

## Resolução do Merge

> **Merged por:** merge-amendment em 2026-03-31
> **Versão base após merge:** UX-008 v0.3.0
> **Alterações aplicadas:** Seção §4 "Spec Visual Penpot" adicionada ao UX-008 com layout visual detalhado (content header, RoutineList refinado, 4 MetricCards, badges com cores spec, novos componentes AutoRefreshIndicator/CollapsibleSection/JSONViewer). Componentes existentes atualizados com detalhes visuais da spec Penpot.

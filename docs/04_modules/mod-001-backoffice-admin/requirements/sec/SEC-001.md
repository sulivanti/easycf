> ⚠️ **ARQUIVO GERIDO POR AUTOMAÇÃO.**
>
> - **Status DRAFT:** Enriqueça o conteúdo deste arquivo diretamente.
> - **Status READY:** NÃO EDITE DIRETAMENTE. Use a skill `create-amendment`.
>
> | Versão | Data       | Responsável | Status/Integração |
> |--------|------------|-------------|-------------------|
> | 0.6.0  | 2026-03-25 | merge-amendment | Merge AMD-SEC-001-001: interceptor HTTP 401 global no api-client.ts — redirect /login em qualquer 401. |
> | 0.5.0  | 2026-03-25 | merge-amendment | Merge SEC-001-M01: catálogo de scopes consumidos expandido de 4 para 17 (sidebar 9 grupos, 12 módulos) — derivado de UX-001-M02. |
> | 0.1.0  | 2026-03-16 | arquitetura | Baseline Inicial (forge-module) |
> | 0.4.0  | 2026-03-17 | AGN-DEV-06  | Enriquecimento Batch 3 — reforça distinção 401/5xx, alinha com BR-009/BR-010/NFR-001, adiciona seção LGPD |
> | 0.3.0  | 2026-03-17 | AGN-DEV-06  | Re-enriquecimento SEC — corrige ref SEC-000, adiciona EX-* |
> | 0.2.0  | 2026-03-16 | AGN-DEV-06  | Enriquecimento SEC (enrich-agent) |

# SEC-001 — Segurança e Compliance do Backoffice Admin

---

## 1. Autenticação

- **Modelo:** Delegada integralmente ao MOD-000 (Foundation)
- **Fluxos no Shell:**
  - Login nativo: e-mail + senha via UX-AUTH-001 → POST /auth/login (auth_login)
  - Recuperação de senha: UX-AUTH-001 painel forgot-password → POST /auth/forgot-password
  - Reset de senha: UX-AUTH-001 painel reset-password → POST /auth/reset-password
  - Alteração de senha: ProfileWidget modal → POST /auth/change-password (auth_change_password, FR-007, INT-006)
  - Logout: ProfileWidget → POST /auth/logout (auth_logout)
- **Sessões:** Gerenciadas pelo MOD-000 (JWT com sessionId ancorado em banco)
- **Tokens:** Cookies httpOnly (accessToken + refreshToken) — **não acessíveis via JavaScript**
- **MFA:** Quando ativo, login exibe Toast informativo + NÃO redireciona (fallback defensivo PENDENTE-004 Opção B — UX-MFA-001 roadmap futuro)

## 2. Autorização

- **Modelo:** RBAC por escopos, consumido via `auth_me.scopes[]` (MOD-000)
- **Sidebar:** Filtrada por scopes — itens sem permissão NÃO aparecem (nem desabilitados) (BR-005)
- **Dashboard shortcuts:** Filtrados por `required_scope` contra `auth_me.scopes[]` (FR-005)
- **Proteção de rotas:** JWT expirado/ausente → redirect /login via interceptor HTTP global
- **Interceptor global (AMD-SEC-001-001):** O `api-client.ts` executa `window.location.href = '/login'` em qualquer response 401, ANTES de lançar `ApiError`. Isso garante redirect independente de como o caller trata o erro. Callers podem remover tratamento 401 redundante.

### Catálogo de Scopes Consumidos pelo MOD-001

| Scope | Recurso | Usado em | Grupo Sidebar |
|---|---|---|---|
| `users:user:read` | Usuários | Sidebar, Shortcuts | Administração |
| `users:role:read` | Perfis e Permissões | Sidebar, Shortcuts | Administração |
| `tenants:branch:read` | Filiais | Sidebar | Administração |
| `org:unit:read` | Estrutura Organizacional | Sidebar, Shortcuts | Organização |
| `identity:org_scope:read` | Escopos Organizacionais | Sidebar | Identidade |
| `identity:share:read` | Compartilhamentos | Sidebar | Identidade |
| `process:cycle:read` | Modelagem de Processos | Sidebar, Shortcuts | Processos |
| `process:case:read` | Casos | Sidebar, Shortcuts | Processos |
| `param:framer:read` | Enquadradores | Sidebar | Parametrização |
| `param:routine:read` | Rotinas | Sidebar | Parametrização |
| `integration:log:read` | Monitor de Integrações | Sidebar | Integrações |
| `integration:routine:write` | Rotinas de Integração | Sidebar | Integrações |
| `approval:movement:read` | Caixa de Entrada | Sidebar, Shortcuts | Aprovações |
| `approval:rule:read` | Regras de Aprovação | Sidebar | Aprovações |
| `mcp:agent:read` | Agentes MCP | Sidebar | Automação |
| `mcp:log:read` | Execuções MCP | Sidebar | Automação |
| `system:audit:read` | Auditoria | Sidebar | Sistema |

> **Anti-pattern (DOC-FND-000 §2):** O MOD-001 NÃO define novos scopes. Todos os 17 scopes são definidos pelos módulos owners e registrados no catálogo canônico DOC-FND-000 §2.2.
>
> **Nota sobre `integration:routine:write`:** O item "Rotinas de Integração" usa scope `:write` (não `:read`) porque a tela é de edição/configuração, não apenas visualização. Usuários com apenas `:read` veem o Monitor mas não as Rotinas.

## 3. Prevenção de Enumeração de Usuários

- **Login (BR-001):** Mensagem DEVE ser sempre "E-mail ou senha inválidos." — sem variações
- **Forgot-password (BR-002):** Mensagem DEVE ser sempre positiva — mesma resposta para e-mail existente e inexistente
- **Validação:** Testes E2E devem confirmar que ambas as telas seguem esta regra

## 4. Classificação dos Dados

| Dado | Classificação | Tráfego | Armazenamento |
|---|---|---|---|
| E-mail (input login/forgot) | Interno | HTTPS | Memória (React state) — nunca localStorage |
| Senha (input login/reset/change-password) | Confidencial | HTTPS | Memória (React state) — limpar após submit (FR-007: limpar ao fechar modal) |
| Nome do usuário (auth_me) | Interno | HTTPS | Memória (React state) — via auth_me (BR-008) |
| Tenant name (auth_me) | Interno | HTTPS | Memória (React state) |
| JWT tokens | Confidencial | Cookies httpOnly | Browser cookie store — não acessível via JS |
| Scopes (auth_me) | Interno | HTTPS | Memória (React state) |

## 5. Telemetria e Privacidade

- **tenant_id AUSENTE** em ações pré-autenticação UX-AUTH-001 (BR-006)
- **tenant_id PRESENTE** em ações pós-autenticação UX-SHELL-001 e UX-DASH-001 (BR-006)
- **Nenhuma PII** no UIActionEnvelope (não inclui e-mail, nome ou senha)
- **correlation_id** propagado em todas as requisições HTTP (rastreabilidade sem PII)

## 6. Proteções

- **Rate limit:** Delegado ao MOD-000 (429 + retry_after) — Shell exibe countdown (FR-001)
- **Sanitização:** Inputs de e-mail e senha sanitizados no frontend (XSS prevention)
- **CSRF:** Cookies sameSite=lax protegem contra CSRF
- **Interceptor HTTP 401:** Global — redireciona para /login em qualquer requisição com sessão expirada (FR-004)

### Distinção 401 vs 5xx (MUST)

O interceptor HTTP global DEVE tratar 401 e 5xx de forma distinta:

| Código | Comportamento | Desconecta? | Referência |
|---|---|---|---|
| 401 | Redirect /login + Toast "Sua sessão expirou. Faça login novamente." | SIM | FR-004, SEC-001 |
| 5xx | Toast com correlationId + botão retry | NÃO | BR-010, FR-005 |
| Timeout > 3s (auth_me) | Skeleton → estado de erro parcial + Toast retry | NÃO | BR-009, FR-005 |

> **Regra:** Apenas o 401 aciona desconexão. Erros 5xx e timeout mantêm o usuário autenticado no Shell (NFR-001 §5).

## 7. LGPD e Retenção

- **Dados em memória:** E-mail e senha armazenados apenas em React state — nunca em localStorage ou sessionStorage (SEC-001 §4)
- **Limpeza pós-submit:** Campos de senha DEVEM ser limpos da memória imediatamente após o submit (login, reset)
- **Nenhuma PII em telemetria:** UIActionEnvelope NÃO inclui e-mail, nome ou senha — apenas action_id, screen_id, status, duration_ms (BR-006, DATA-003)
- **Retenção de domain events:** Gerenciada pelo MOD-000 (Foundation) — MOD-001 não persiste eventos
- **Direito de esquecimento:** Delegado ao MOD-000 — MOD-001 reflete apenas dados retornados por auth_me em memória

- **estado_item:** READY
- **owner:** arquitetura
- **data_ultima_revisao:** 2026-03-23
- **rastreia_para:** US-MOD-001-F01, US-MOD-001-F02, US-MOD-001-F03, BR-001, BR-002, BR-005, BR-006, BR-008, BR-009, BR-010, FR-001, FR-004, FR-005, FR-006, FR-007, INT-006, SEC-002, DATA-003, NFR-001, DOC-FND-000
- **referencias_exemplos:** EX-CI-006, EX-CI-007
- **evidencias:** N/A

> ⚠️ **ARQUIVO GERIDO POR AUTOMAÇÃO.**
> - Emenda sobre documento normativo/requisito em estado READY.
> - Para novas emendas, use a skill `create-amendment`.

# Emenda: FR-000-C10

- **Documento base:** [FR-000](../../requirements/fr/FR-000.md)
- **estado_item:** MERGED
- **Natureza:** C (Correção)
- **Data:** 2026-03-31
- **owner:** ECF Core
- **Motivação:** O scope `mcp:agent:phase2-enable` no seed-admin.ts contém hífen que viola a regex canônica do VO `Scope`. O DATA-000-C03 relaxou o CHECK constraint como workaround, mas a correção correta é renomear o scope e reverter o constraint à regex canônica.
- **rastreia_para:** spec-fix-scope-hyphen-rename, DOC-FND-000-C01, DATA-000-C03

---

## Detalhamento

### Alterações em Código

**1. seed-admin.ts** — Renomear scope:
```typescript
// Antes
'mcp:agent:phase2-enable',
// Depois
'mcp:agent:phase2_enable',
```

**2. schema/foundation.ts** — Reverter CHECK constraint para regex canônica (desfazer DATA-000-C03):
```typescript
// Antes (DATA-000-C03 — com hífens)
sql`${table.scope} ~ '^[a-z][a-z0-9_-]*(:[a-z][a-z0-9_-]*){1,2}$'`

// Depois (regex canônica original — sem hífens)
sql`${table.scope} ~ '^[a-z][a-z0-9_]*(:[a-z][a-z0-9_]*){1,2}$'`
```

**3. Migration SQL** (executar no banco de produção antes do deploy):
```sql
UPDATE role_permissions SET scope = 'mcp:agent:phase2_enable' WHERE scope = 'mcp:agent:phase2-enable';
```

### Anotação no Done Funcional de FR-007 (Seed Admin)

Adicionar nota: "O scope `mcp:agent:phase2_enable` foi corrigido de `phase2-enable` (hífen) para `phase2_enable` (underscore) — FR-000-C10. Hífens não são aceitos pela regex canônica do VO Scope."

### Arquivos Afetados

| # | Arquivo | Ação |
|---|---|---|
| 1 | `apps/api/db/seed-admin.ts` | Renomear scope |
| 2 | `apps/api/db/schema/foundation.ts` | Reverter CHECK constraint |

---

## Impacto nos Pilares

- **Pilares afetados:** DATA (reversão do CHECK constraint — desfaz DATA-000-C03)
- **Ação requerida:** Nenhum amendment derivado necessário — a reversão do CHECK é consequência direta do rename.

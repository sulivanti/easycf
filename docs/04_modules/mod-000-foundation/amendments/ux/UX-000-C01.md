> ⚠️ **ARQUIVO GERIDO POR AUTOMAÇÃO.**
> - Emenda sobre documento normativo/requisito em estado READY.
> - Para novas emendas, use a skill `create-amendment`.

# Emenda: UX-000-C01

- **Documento base:** [UX-000](../../requirements/ux/UX-000.md)
- **estado_item:** MERGED
- **Natureza:** C (Correção)
- **Data:** 2026-03-25
- **owner:** arquitetura
- **Motivação:** A tabela "Tratamento de Erros e Mensagens" em UX-000 descreve o comportamento do HTTP 401 como "Redirect para /login com mensagem Sessão expirada", mas não especifica que é necessário limpar `localStorage`, limpar cache React Query e forçar full reload. Sem essas etapas, ocorre um loop de redirecionamento que impede o acesso à tela de login.
- **rastreia_para:** UX-001, FR-000-C06, spec-fix-session-timeout-redirect-loop

---

## Detalhamento

### Correção na tabela "Tratamento de Erros e Mensagens" (UX-000 §Tratamento de Erros)

**Antes:**

| HTTP Status | Comportamento UX |
|---|---|
| **401** | Redirect para /login com mensagem "Sessão expirada" |

**Depois:**

| HTTP Status | Comportamento UX |
|---|---|
| **401** | (1) Toast único "Sua sessão expirou. Faça login novamente." com `id` fixo para deduplicação; (2) Limpar `localStorage['auth_tokens']`; (3) Limpar cache React Query (`queryClient.clear()`); (4) Full reload para `/login` via `window.location.href` (necessário para resetar router context estático). NÃO usar `navigate()` — causa redirect loop. |

### Adição no UX-001 — Jornada Alternativa: Sessão Expirada por Timeout

Nova seção a ser inserida após "Jornada Alternativa — Recuperação de Senha":

```markdown
### Jornada Alternativa — Sessão Expirada por Timeout

1. Token JWT expira no servidor (12h normal, 30d remember_me)
2. AppShell tenta GET /auth/me → recebe HTTP 401
3. Toast "Sua sessão expirou. Faça login novamente." (id fixo, sem duplicatas)
4. `forceLogout()`: limpa localStorage + cache React Query + full reload `/login`
5. Tela de login carrega normalmente — sem redirect automático de volta
6. Usuário faz login novamente → fluxo normal (UX-001 Happy Path)
```

### Adição na tabela "Alternativas e Erros" do UX-001

| Situação | Comportamento |
|---|---|
| Sessão expirada (timeout 12h/30d) | Toast único "Sua sessão expirou. Faça login novamente." → forceLogout (limpa tokens + cache + full reload /login) |

---

## Impacto nos Pilares

- **Pilares afetados:** FR (FR-000-C06 — implementação do forceLogout)
- **Ação requerida:** Nenhuma ação adicional — FR-000-C06 já cobre a implementação. Este amendment apenas alinha a documentação UX com o comportamento correto.

---

## Resolução do Merge

> **Merged por:** merge-amendment (batch) em 2026-03-25
> **Versão base após merge:** UX-000 v0.3.1
> **Alterações aplicadas:** Tabela erros §401 detalhada com forceLogout, nova jornada "Sessão Expirada por Timeout" em UX-001, nova linha na tabela "Alternativas e Erros"

> **ARQUIVO GERIDO POR AUTOMACAO.**
> - Emenda sobre documento normativo/requisito em estado READY.
> - Para novas emendas, use a skill `create-amendment`.

# Emenda: SEC-001-M01

- **Documento base:** [SEC-001](../../requirements/sec/SEC-001.md)
- **estado_item:** MERGED
- **Natureza:** M (Melhoria)
- **Data:** 2026-03-30
- **owner:** arquitetura
- **Motivacao:** Adicionar escopos RBAC para a nova entidade departamentos (CRUD independente). Os 3 escopos `org:dept:*` seguem o mesmo padrão de `org:unit:*` já existente em SEC-001 §2, mas para a entidade `departments` que é isolada por tenant (diferente de org_units que é cross-tenant).
- **rastreia_para:** FR-002, DATA-002, BR-002, SEC-002-M01, PEN-003/PENDENTE-008

---

## Detalhamento

### SEC-001 §2 — Novos Escopos de Departamentos

Adicionar à tabela de escopos do módulo (SEC-001 §2):

| Escopo | Descrição | Endpoints protegidos |
|---|---|---|
| `org:dept:read` | Visualizar departamentos | GET /departments, GET /departments/:id |
| `org:dept:write` | Criar, editar e restaurar departamentos | POST /departments, PATCH /departments/:id, PATCH /departments/:id/restore |
| `org:dept:delete` | Desativar departamentos | DELETE /departments/:id |

**Formato de scopes:** `dominio:entidade:acao` (3 segmentos) — conforme DOC-FND-000 §2.1.

**Registro:** Novos scopes DEVEM ser registrados no catálogo canônico DOC-FND-000 §2.2 (seed do MOD-000).

**Regra:** Requisição sem scope válido retorna 403 RFC 9457 com `type="/problems/forbidden"`.

### SEC-001 §3 — Classificação (Departamentos)

- **Classificação:** `Interno` — dados de departamentos não contêm PII diretamente
- **Sensitivity level:** 0 (dados organizacionais não-sensíveis)

### SEC-001 §7 — Isolamento de Departamentos (por tenant)

> **Nota:** Departamentos são isolados **por tenant** (com `tenant_id`), diferente de org_units que é cross-tenant (ADR-003). Todas as queries DEVEM filtrar por `tenant_id` do JWT. Não é necessário RLS; o filtro é aplicado na camada de repository.

- **MUST:** Toda query de departamentos DEVE incluir `WHERE tenant_id = :tenantId` (extraído do JWT)
- **MUST:** Departamento de outro tenant → 404 (não 403, para não expor existência)
- **MUST:** `tenant_id` é derivado do JWT — nunca aceito no body da requisição

---

## Impacto

- SEC-001 §2: +3 linhas na tabela de escopos
- SEC-001 §3: menção a departamentos na classificação
- SEC-001 §7: nova seção de isolamento por tenant (departamentos)
- DOC-FND-000 §2.2: registro dos 3 novos scopes no catálogo canônico

---

## Resolução

> **Status:** MERGED em 2026-03-31. Base SEC-001 bumped v0.3.0.

> ⚠️ **ARQUIVO GERIDO POR AUTOMAÇÃO.**
> - Emenda sobre documento base em estado READY.
> - Para novas emendas, use a skill `create-amendment`.

# Emenda: UX-001-M02

- **Documento base:** [UX-001](../../requirements/ux/UX-001.md)
- **estado_item:** MERGED
- **status_implementacao:** IMPLEMENTADO
- **Natureza:** M (Melhoria)
- **Data:** 2026-03-25
- **owner:** arquitetura
- **Motivação:** O app tem 31 page components implementados nos 12 módulos mas apenas 4 rotas conectadas (/, /login, /_auth, /dashboard) e 4 itens no sidebar. Esta emenda expande a navegação completa: sidebar com 9 grupos cobrindo 12 módulos, 20+ route files, breadcrumbs expandidos, ProfileWidget com links de perfil/sessões, e shortcuts do dashboard para módulos principais.
- **rastreia_para:** UX-SHELL-001, UX-DASH-001, FR-004, FR-005, BR-005, DOC-UX-011 CA-07, DOC-UX-011-M01 CA-09

---

## Detalhamento

### 1. Sidebar expandida — 9 grupos cobrindo 12 módulos

Substituir o `SIDEBAR_GROUPS` em `sidebar-config.ts` por 9 grupos:

| # | Grupo (id) | Label | Ícone | Itens |
|---|-----------|-------|-------|-------|
| 1 | `admin` | Administração | `settings` | Usuários (`/usuarios`, `users:user:read`), Perfis e Permissões (`/perfis`, `users:role:read`), Filiais (`/filiais`, `tenants:branch:read`) |
| 2 | `org` | Organização | `network` | Estrutura Organizacional (`/org-units`, `org:unit:read`) |
| 3 | `identity` | Identidade | `fingerprint` | Escopos Organizacionais (`/identity/org-scope`, `identity:org_scope:read`), Compartilhamentos (`/identity/delegations`, `identity:share:read`) |
| 4 | `process` | Processos | `workflow` | Modelagem (`/processos/ciclos`, `process:cycle:read`), Casos (`/cases`, `process:case:read`) |
| 5 | `params` | Parametrização | `sliders` | Enquadradores (`/framers`, `param:framer:read`), Rotinas (`/routines`, `param:routine:read`) |
| 6 | `integration` | Integrações | `plug` | Monitor (`/integration/monitor`, `integration:log:read`), Rotinas de Integração (`/integration/routines`, `integration:routine:write`) |
| 7 | `approval` | Aprovações | `inbox` | Caixa de Entrada (`/approvals/inbox`, `approval:movement:read`), Regras (`/approvals/config`, `approval:rule:read`) |
| 8 | `mcp` | Automação | `bot` | Agentes MCP (`/mcp/agents`, `mcp:agent:read`), Execuções (`/mcp/executions`, `mcp:log:read`) |
| 9 | `system` | Sistema | `cpu` | Auditoria (`/auditoria`, `system:audit:read`) |

**Nota:** MOD-011 (SmartGrid) NÃO tem entrada na sidebar — é componente invocado por outros módulos.

### 2. AppShell — ICONS expandido

Adicionar ao mapa `ICONS` (`AppShell.tsx:31-38`):

| Key | Lucide Icon |
|-----|-------------|
| `network` | `Network` |
| `fingerprint` | `Fingerprint` |
| `workflow` | `Workflow` |
| `briefcase` | `Briefcase` |
| `sliders` | `SlidersHorizontal` |
| `plug` | `Plug` |
| `inbox` | `Inbox` |
| `bot` | `Bot` |

### 3. AppShell — ROUTE_LABELS expandido

Adicionar ao mapa `ROUTE_LABELS` (`AppShell.tsx:126-132`):

```typescript
const ROUTE_LABELS: Record<string, string> = {
  // existentes
  dashboard: 'Dashboard',
  usuarios: 'Usuários',
  perfis: 'Perfis e Permissões',
  filiais: 'Filiais',
  auditoria: 'Auditoria',
  // novos
  profile: 'Meu Perfil',
  sessoes: 'Sessões Ativas',
  'org-units': 'Estrutura Organizacional',
  identity: 'Identidade',
  'org-scope': 'Escopos Organizacionais',
  delegations: 'Compartilhamentos',
  processos: 'Processos',
  ciclos: 'Ciclos',
  editor: 'Editor',
  cases: 'Casos',
  framers: 'Enquadradores',
  routines: 'Rotinas',
  integration: 'Integrações',
  monitor: 'Monitor',
  approvals: 'Aprovações',
  inbox: 'Caixa de Entrada',
  config: 'Configuração',
  mcp: 'Automação',
  agents: 'Agentes',
  executions: 'Execuções',
  form: 'Formulário',
};
```

### 4. Route files — 20 novas rotas

#### Tipo A — Zero-prop (import direto)

| Route File | Path | Componente | Import |
|-----------|------|------------|--------|
| `_auth.profile.tsx` | `/profile` | `ProfilePage` | `@modules/foundation/pages/profile/ProfilePage` |
| `_auth.sessoes.tsx` | `/sessoes` | `SessionsPage` | `@modules/foundation/pages/sessions/SessionsPage` |
| `_auth.perfis.tsx` | `/perfis` | `RolesPage` | `@modules/foundation/pages/roles/RolesPage` |
| `_auth.filiais.tsx` | `/filiais` | `TenantsPage` | `@modules/foundation/pages/tenants/TenantsPage` |
| `_auth.mcp.agents.tsx` | `/mcp/agents` | `AgentsPage` | `@modules/mcp-automation/pages/agents/AgentsPage` |
| `_auth.mcp.executions.tsx` | `/mcp/executions` | `ExecutionsPage` | `@modules/mcp-automation/pages/executions/ExecutionsPage` |
| `_auth.approvals.config.tsx` | `/approvals/config` | `RulesConfigPage` | `@modules/movement-approval/pages/config/RulesConfigPage` |

Padrão:
```typescript
import { createRoute } from '@tanstack/react-router';
import { Route as authRoute } from './_auth';
import { ComponentName } from '@modules/...';

export const Route = createRoute({
  path: '/path',
  getParentRoute: () => authRoute,
  component: ComponentName,
});
```

#### Tipo B — Wrapper com useAuthMe (userScopes)

| Route File | Path | Componente | Props |
|-----------|------|------------|-------|
| `_auth.usuarios.tsx` | `/usuarios` | `UsersListPage` | `onCreateClick → navigate('/usuarios/form')`, `onEditClick → navigate('/usuarios/form?id=...')` |
| `_auth.org-units.tsx` | `/org-units` | `OrgTreePage` | `userScopes`, `onNavigateCreate → navigate('/org-units/form')`, `onNavigateEdit → navigate('/org-units/form?mode=edit&editId=...')`, `onNavigateHistory` |
| `_auth.identity.org-scope.tsx` | `/identity/org-scope` | `OrgScopeManagementPage` | `userId, userName, userScopes` (de `useAuthMe`) |
| `_auth.identity.delegations.tsx` | `/identity/delegations` | `SharesDelegationsPanelPage` | `userScopes, currentUserId, availableScopes` (de `useAuthMe`) |
| `_auth.processos.ciclos.tsx` | `/processos/ciclos` | `CyclesListPage` | `userScopes` |
| `_auth.cases.tsx` | `/cases` | `CaseListPage` | `userScopes, onSelectCase → navigate('/cases/$id')` |
| `_auth.framers.tsx` | `/framers` | `FramersConfigPage` | `userScopes` |
| `_auth.routines.tsx` | `/routines` | `RoutinesEditorPage` | `userScopes` |
| `_auth.integration.monitor.tsx` | `/integration/monitor` | `IntegrationMonitorPage` | `userScopes` |
| `_auth.integration.routines.tsx` | `/integration/routines` | `RoutineEditorPage` | `userScopes` |
| `_auth.approvals.inbox.tsx` | `/approvals/inbox` | `ApprovalInboxPage` | `currentUserId` (de `useAuthMe`) |
| `_auth.auditoria.tsx` | `/auditoria` | `ComingSoonPage` | (não existe AuditPage) |

Padrão:
```typescript
import { createRoute, useNavigate } from '@tanstack/react-router';
import { Route as authRoute } from './_auth';
import { useAuthMe } from '@modules/backoffice-admin/hooks/use-auth-me';
import { ComponentName } from '@modules/...';

export const Route = createRoute({
  path: '/path',
  getParentRoute: () => authRoute,
  component: PageWrapper,
});

function PageWrapper() {
  const { data: user } = useAuthMe();
  const navigate = useNavigate();
  if (!user) return null;
  return <ComponentName userScopes={user.scopes} /* callbacks */ />;
}
```

#### Tipo C — Com parâmetros de URL

| Route File | Path | Componente | Params |
|-----------|------|------------|--------|
| `_auth.processos.ciclos.$id.editor.tsx` | `/processos/ciclos/$id/editor` | `FlowEditorPage` | `cycleId` (de `useParams`) + `userScopes` |
| `_auth.cases.$id.tsx` | `/cases/$id` | `CasePanelPage` | `caseId` (de `useParams`) + `userScopes` |
| `_auth.usuarios.form.tsx` | `/usuarios/form` | `UserFormPage` | `userId` via searchParams, `onNavigateToList → navigate('/usuarios')` |
| `_auth.org-units.form.tsx` | `/org-units/form` | `OrgFormPage` | `mode, editId, parentId` via searchParams |

### 5. routeTree.ts — Registrar todas as rotas

Atualizar `apps/web/src/routeTree.ts` para importar e registrar os ~24 route files como children de `authRoute`.

### 6. index.tsx — Redirect inteligente

O root `/` deve redirecionar para `/dashboard` se autenticado, senão `/login`:

```typescript
beforeLoad: ({ context }) => {
  throw redirect({ to: context.auth?.user ? '/dashboard' : '/login' });
},
```

### 7. ProfileWidget — Links de navegação

Adicionar ao dropdown do `ProfileWidget` (`ProfileWidget.tsx`), entre "Alterar Senha" e "Sair":

- "Meu Perfil" → `/profile` (icon: `User`)
- "Sessões Ativas" → `/sessoes` (icon: `Monitor`)

Usar `useNavigate()` do TanStack Router para navegação.

### 8. shortcut-config.ts — Shortcuts expandidos

Adicionar ao `SHORTCUT_CATALOG`:

| id | label | description | icon | route | requiredScope |
|----|-------|-------------|------|-------|---------------|
| `shortcut-org` | Estrutura Organizacional | Gerencie a árvore organizacional | `network` | `/org-units` | `org:unit:read` |
| `shortcut-process` | Processos | Modele ciclos e fluxos de trabalho | `workflow` | `/processos/ciclos` | `process:cycle:read` |
| `shortcut-cases` | Casos | Acompanhe casos em andamento | `briefcase` | `/cases` | `process:case:read` |
| `shortcut-approvals` | Aprovações | Gerencie aprovações pendentes | `inbox` | `/approvals/inbox` | `approval:movement:read` |

### Gherkin

```gherkin
Cenário: Sidebar exibe 9 grupos filtrados por scopes
  Dado que o usuário está autenticado com scopes ["users:user:read", "org:unit:read", "process:cycle:read"]
  Quando acessa /dashboard
  Então a sidebar DEVE exibir os grupos "Administração", "Organização" e "Processos"
  E NÃO DEVE exibir grupos sem itens com scope correspondente

Cenário: Navegar para módulo real via sidebar
  Dado que o usuário está autenticado com scope "org:unit:read"
  Quando clica no item "Estrutura Organizacional" na sidebar
  Então DEVE navegar para /org-units
  E DEVE renderizar o componente OrgTreePage
  E NÃO DEVE exibir ComingSoonPage

Cenário: Rota /auditoria exibe ComingSoonPage
  Dado que o usuário está autenticado com scope "system:audit:read"
  Quando navega para /auditoria
  Então DEVE exibir a página "Módulo em construção"
  E DEVE exibir botão "Voltar ao Dashboard"

Cenário: ProfileWidget exibe links de navegação
  Dado que o usuário está autenticado
  Quando clica no avatar/nome no header
  Então o dropdown DEVE exibir "Meu Perfil", "Sessões Ativas", "Alterar Senha" e "Sair"
  E clicar em "Meu Perfil" DEVE navegar para /profile
  E clicar em "Sessões Ativas" DEVE navegar para /sessoes

Cenário: Dashboard exibe shortcuts de módulos principais
  Dado que o usuário está autenticado com scopes ["org:unit:read", "process:cycle:read"]
  Quando acessa /dashboard
  Então DEVE exibir cards "Estrutura Organizacional" e "Processos"
  E clicar no card DEVE navegar para a rota correspondente

Cenário: Breadcrumbs refletem rota aninhada
  Dado que o usuário está em /processos/ciclos
  Então o breadcrumb DEVE exibir "Início / Processos / Ciclos"
  E cada segmento DEVE exibir o label correto do ROUTE_LABELS

Cenário: Index redirect inteligente
  Dado que o usuário NÃO está autenticado
  Quando acessa /
  Então DEVE redirecionar para /login
  Dado que o usuário está autenticado
  Quando acessa /
  Então DEVE redirecionar para /dashboard
```

---

## Impacto nos Pilares

- **Pilares afetados:** UX (sidebar, breadcrumbs, ProfileWidget, route files), FR (codegen output, wiring), SEC (scopes consumidos)
- **Ação requerida:**
  1. Modificar `sidebar-config.ts` — 9 grupos com ~17 itens
  2. Modificar `shortcut-config.ts` — 4 novos shortcuts
  3. Modificar `AppShell.tsx` — ICONS expandido (8 novos), ROUTE_LABELS expandido (19 novos)
  4. Modificar `ProfileWidget.tsx` — 2 links de navegação
  5. Modificar `index.tsx` — redirect inteligente
  6. Modificar `routeTree.ts` — importar e registrar ~24 rotas
  7. Criar 20 route files em `apps/web/src/routes/`

---

## Amendments Derivados

| ID | Pilar | Módulo | Natureza | Estado |
|---|---|---|---|---|
| [FR-001-M01](../fr/FR-001-M01.md) | FR | mod-001 | M | DRAFT |
| [SEC-001-M01](../sec/SEC-001-M01.md) | SEC | mod-001 | M | DRAFT |

### Revisões Humanas Pendentes

| Pilar | Módulo | Ação |
|---|---|---|
| INT-005 | mod-001 | REVIEW — verificar se rotas novas precisam de timeouts diferenciados por endpoint |

---

## Resolução do Merge

> **Merged por:** merge-amendment em 2026-03-25
> **Versão base após merge:** UX-001 v0.7.0
> **Alterações aplicadas:** UX-002 (sidebar 9 grupos, ProfileWidget +2 links), UX-003 (shortcuts +4), nova UX-004 (24 route files, 3 padrões, ROUTE_LABELS, regras CA-07/CA-08/CA-09)
> **Amendments derivados:** FR-001-M01, SEC-001-M01 (DRAFT — merge após este pai)

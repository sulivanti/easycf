> ⚠️ **ARQUIVO GERIDO POR AUTOMAÇÃO.**
>
> - **Status DRAFT:** Enriqueça o conteúdo deste arquivo diretamente.
> - **Status READY:** NÃO EDITE DIRETAMENTE. Use a skill `create-amendment`.
>
> | Versão | Data       | Responsável | Status/Integração |
> |--------|------------|-------------|-------------------|
> | 0.3.0  | 2026-04-01 | merge-amendment | Merge UX-011-M01: layouts pixel-perfect para 3 views (BulkInsert, RecordEdit, BulkDelete), ImportJsonModal, ReadOnlyField, GridFooter, ValidationResultsPanel, MetadataSidebar, DeleteConfirmation split-view, row status indicators, responsividade |
> | 0.2.0  | 2026-03-19 | AGN-DEV-07  | Enriquecimento UX — 3 telas, jornadas, ações DOC-UX-010, componentes, estados, copy, cenários de erro |
> | 0.1.0  | 2026-03-19 | arquitetura | Baseline Inicial (forge-module) |

# UX-011 — Jornadas e Fluxos do SmartGrid

- **estado_item:** READY
- **owner:** arquitetura
- **data_ultima_revisao:** 2026-04-01
- **rastreia_para:** US-MOD-011, US-MOD-011-F02, US-MOD-011-F03, US-MOD-011-F04, US-MOD-011-F05, FR-011, BR-011, SEC-011, NFR-011, DOC-UX-010
- **referencias_exemplos:** N/A
- **evidencias:** N/A

---

> **Nota arquitetural:** MOD-011 é Nível 1 (UX Consumer). Todas as 3 telas consomem o motor do MOD-007 para renderização dinâmica e validação. Não há formulários de configuração no MOD-011 (configuração de Operações permanece no MOD-007 via UX-ROTINA-001).

---

## Telas

| Manifest | Tela | Feature | Route | Tipo |
|---|---|---|---|---|
| UX-SGR-001 | Grade de Inclusão em Massa | F02, F05 | `/{modulo}/{rotina}/inclusao-em-massa` | grid |
| UX-SGR-002 | Formulário de Alteração de Registro | F03 | `/{modulo}/{rotina}/{id}/alterar` | form |
| UX-SGR-003 | Grade de Exclusão em Massa | F04 | `/{modulo}/{rotina}/exclusao-em-massa` | grid |

## Manifests Vinculados

- [ux-sgr-001.inclusao-massa.yaml](../../../../05_manifests/screens/ux-sgr-001.inclusao-massa.yaml)
- [ux-sgr-002.alteracao-registro.yaml](../../../../05_manifests/screens/ux-sgr-002.alteracao-registro.yaml)
- [ux-sgr-003.exclusao-massa.yaml](../../../../05_manifests/screens/ux-sgr-003.exclusao-massa.yaml)

---

## UX-SGR-001 — Grade de Inclusão em Massa

- **Screen ID:** UX-SGR-001
- **Entidade(s) principal(is):** `{entity_type}` do módulo destino (dinâmico)
- **Contexto:** Grade editável com inclusão em massa de novos registros
- **Features:** US-MOD-011-F02, US-MOD-011-F05

### Layout Visual (ref: UX-011-M01, spec 95-smartgrid)

```
ContentArea (1200x836, fill #F5F5F3, padding:24px)
  PageHeader (flex justify-between, align-center)
    Esquerda
      'Grade de Inclusao em Massa' (24px 800 #111111)
      'Operacao: {nome_operacao}' (13px 400 #888888, mt:4px)
    Direita (flex, gap:8px)
      BtnImport (secondary: r:8, border 1px #E8E8E6, h:36) 'Import JSON' (12px 600 #555555)
      BtnExport (secondary: r:8, border 1px #E8E8E6, h:36) 'Export JSON' (12px 600 #555555)
  Toolbar (flex justify-between, align-center, mt:16px)
    Esquerda (flex, gap:8px)
      BtnNovaLinha (text: r:8, h:36, no border, bg transparent) '+ Nova Linha' (12px 700 #2E86C1)
      BtnAcoesLote (secondary: r:8, border 1px #E8E8E6, h:36) 'Acoes em Lote' + chevron-down
        Dropdown: 'Aplicar valor em coluna' | 'Limpar coluna' | 'Duplicar linhas selecionadas'
    Direita: '{N} linhas' (12px 400 #888888)
  GridCard (fill #FFF, r:12, border 1px #E8E8E6, mt:12px, overflow hidden)
    GridHeader (h:44, bg #FAFAFA, border-bottom 1px #F0F0EE, p:0 16px)
      Checkbox (w:18, col-ck w:36) | STATUS (w:40, icone only) | 'CODIGO' (w:120) | 'DESCRICAO' (flex:1) | 'VALOR' (w:100, right) | 'QUANTIDADE' (w:100, right) | 'TIPO' (w:120) | 'DEPARTAMENTO' (w:140)
    GridRow (h:48, border-bottom 1px #F0F0EE, p:0 16px)
      border-left: 3px solid (VALID:#27AE60 | WARNING:#F39C12 | ERROR:#E74C3C)
      Cells click-to-edit (13px 500 #111111), em edicao: border 2px solid #2E86C1
      Cell alerta: bg #FFF8E1, text #E67E22 | Cell erro: bg #FFEBEE, text #E74C3C
    GridFooter (h:52, p:0 16px, flex justify-between, bg #FAFAFA, border-top 1px #F0F0EE)
      '{X} de {Y} linhas validas' (13px 500 #555555)
      BtnSubmeter (primary: r:8, fill #2E86C1, h:40) 'Submeter Lote' (13px 700 #FFFFFF)
        Disabled se erros: fill #E8E8E6, text #CCCCCC, cursor not-allowed
```

### Jornada (Happy Path)

1. Usuário navega para `/{modulo}/{rotina}/inclusao-em-massa`
2. Tela exibe skeleton enquanto carrega configuração da Operação (action: `load_operation_config`)
3. Motor retorna `visible_fields`, `required_fields`, `defaults`, `domain_restrictions`
4. Grade renderiza colunas conforme `visible_fields`, com asterisco em `required_fields` e defaults pré-preenchidos
5. Usuário adiciona linhas (action: `add_line`) e preenche valores
6. Usuário aciona "Validar Tudo" — motor chamado 1 linha por vez (action: `validate_line`)
7. Cada linha recebe status visual: ✅ válida, ❌ bloqueante, ⚠️ alerta
8. Com 100% ✅, botão "Salvar" é habilitado (BR-001)
9. Usuário aciona "Salvar" (action: `save_batch`) — registros criados no módulo destino
10. Feedback de sucesso com contagem

### Alternativas e Cenários de Erro

| Cenário | Comportamento | Referência |
|---|---|---|
| Motor retorna erro no mount (403/5xx) | Toast com mensagem de erro + `correlation_id`. Grade não carrega. | FR-002 |
| Linha com `blocking_validations` não vazio | Ícone ❌ com mensagem do motor. Save desabilitado. | BR-001, FR-003 |
| Linha com `validations` sem blocking | Ícone ⚠️. Save permitido (alerta informativo). | FR-003 |
| Nenhuma linha validada (status neutro) | Save desabilitado até validação. | BR-001 |
| Limite de linhas atingido | Botão "Adicionar" desabilitado. Mensagem explicativa. | FR-005, NFR-002 |
| Import JSON com Operação diferente | Aviso ao usuário. Grade populada, status neutro. | FR-004 |
| Import JSON acima do limite de linhas | Rejeição com mensagem explicativa. | FR-005, SEC-011 §7 |
| Import JSON com schema inválido | Rejeição com mensagem explicativa. | SEC-011 §7 |
| Fechar com dados não salvos | Modal de confirmação: "Exportar e sair" / "Sair sem exportar" / "Cancelar" | FR-004 |
| Erro ao salvar lote (parcial ou total) | Toast com contagem de sucesso/falha + `correlation_id`. | FR-009 |

### Estados da Tela

| Estado | Visual | Condição |
|---|---|---|
| Loading | Skeleton (grade completa) | Mount — carregando config do motor |
| Empty | Grade com 0 linhas + botão "Adicionar primeira linha" | Config carregada, nenhuma linha |
| Filling | Grade com N linhas, status neutro (sem ícone) | Linhas adicionadas, não validadas |
| Validating | Spinner por linha sendo validada | "Validar Tudo" em andamento |
| Validated (mixed) | Mix de ✅ ❌ ⚠️ por linha. Save desabilitado se ❌ existir. | Validação completa com erros |
| Validated (all OK) | Todas ✅. Save habilitado. | 100% válidas |
| Saving | Button loading no "Salvar" | Save em andamento |
| Error (mount) | Toast + grade vazia | Falha ao carregar config do motor |

### Ações Selecionadas (do DOC-UX-010)

#### Ações Disponíveis

| action_key | kind | scope | operation_ids | endpoints | domain_events | telemetria |
|---|---|---|---|---|---|---|
| `bulk_create` | command | bulk | `routine_engine_evaluate`, operationId do módulo destino | POST /api/v1/routine-engine/evaluate, POST /{modulo}/{entity} | `{modulo}.{entity}.created`, `param.routine_applied` | UIActionEnvelope com `screen_id: UX-SGR-001` |
| `create` | command | single | operationId do módulo destino | POST /{modulo}/{entity} | `{modulo}.{entity}.created` | UIActionEnvelope |

#### Ações em Massa

| action_key | kind | scope | operation_ids | endpoints | domain_events | telemetria |
|---|---|---|---|---|---|---|
| `bulk_select` | view | bulk | — (UI-only) | — | — | UIActionEnvelope (quantidade selecionada) |
| `bulk_update` | command | bulk | — (client-only: aplicar valor, limpar coluna) | — | — | UIActionEnvelope (action: `apply_value_mass`, `clear_column_mass`) |
| `clone` | command | single | — (client-only: duplicar linha) | — | — | UIActionEnvelope (action: `duplicate_line`) |

### Mapeamento Ações do Manifest → DOC-UX-010

| action_id (manifest) | action_key (DOC-UX-010) | Tipo | Notas |
|---|---|---|---|
| `load_operation_config` | `view` (query) | API | Carrega config no mount |
| `validate_line` | custom (sem equivalente direto) | API | Validação 1-por-1 via motor |
| `save_batch` | `bulk_create` | API | Save de todas linhas válidas |
| `export_json` | `export` (output, client_only) | Client | JSON local, sem servidor |
| `import_json` | `import` (command, client_only) | Client | JSON local, sem servidor |
| `add_line` | `create` (client_only) | Client | Adiciona linha vazia |
| `remove_selected_lines` | `delete` (client_only) | Client | Remove linhas da grade |
| `apply_value_mass` | `bulk_update` (client_only) | Client | Preenche coluna em selecionadas |
| `clear_column_mass` | `bulk_update` (client_only) | Client | Limpa coluna em selecionadas |
| `duplicate_line` | `clone` (client_only) | Client | Duplica 1 linha N vezes |

### Componentes (do Manifest)

| component_id | Componente | Descrição | Ações usadas |
|---|---|---|---|
| `grid-header` | SmartGridHeader | Header com nome da Operação, contadores (linhas/válidas/erros), botões "Importar JSON", "Exportar JSON", "Validar Tudo", "Salvar" | `export_json`, `import_json`, `save_batch` |
| `mass-action-toolbar` | MassActionToolbar | Toolbar OCULTA sem seleção. Aparece com >=1 linha selecionada. Botões "Aplicar valor", "Limpar coluna", "Duplicar item" (1 linha only). | `apply_value_mass`, `clear_column_mass`, `duplicate_line` |
| `data-grid` | SmartDataGrid | Grade editável com colunas por `visible_fields`, checkbox de seleção, coluna de status (✅/⚠️/❌). Cells com defaults, required asterisco, domain dropdowns. | `load_operation_config`, `validate_line`, `add_line`, `remove_selected_lines` |
| `close-confirmation-modal` | CloseConfirmationModal | Modal ao fechar com dados não salvos: "Exportar e sair" / "Sair sem exportar" / "Cancelar". | `export_json` |

### Copy (Mensagens)

| Contexto | Mensagem (pt-BR) |
|---|---|
| Sucesso (save_batch) | "{N} registros incluídos com sucesso." |
| Erro (save_batch parcial) | "{X} de {N} registros falharam. Verifique os erros e tente novamente." |
| Erro (motor no mount) | "Não foi possível carregar a configuração da Operação." |
| Erro (validate_line) | "Erro ao validar linha." |
| Empty state | "Nenhuma linha adicionada. Clique em 'Adicionar linha' para começar." |
| Limite atingido | "Limite máximo de {MAX} linhas atingido." |
| Modal fechar | "Você possui dados não salvos. Deseja exportar antes de sair?" |
| Import JSON Operação diferente | "O arquivo importado foi gerado para outra Operação. Os dados serão carregados, mas devem ser revalidados." |
| Import JSON schema inválido | "O arquivo selecionado não é um JSON válido do SmartGrid." |
| Import JSON acima do limite | "O arquivo contém {N} linhas, excedendo o limite de {MAX}." |

---

## UX-SGR-002 — Formulário de Alteração de Registro

- **Screen ID:** UX-SGR-002
- **Entidade(s) principal(is):** `{entity_type}` do módulo destino (dinâmico)
- **Contexto:** Formulário de edição de registro existente, com validação por status via motor
- **Features:** US-MOD-011-F03

### Layout Visual (ref: UX-011-M01, spec 95-smartgrid)

```
ContentArea (1200x836, fill #F5F5F3, padding:24px)
  TopActions (flex justify-between, align-center)
    BtnVoltar (text: flex, gap:6px) IconeSeta + 'Voltar' (13px 600 #555555)
    BtnValidar (primary: r:8, fill #2E86C1, h:36) 'Validar' (12px 700 #FFFFFF)
  FormLayout (flex, gap:24px, mt:16px)
    FormCard (flex:1, fill #FFF, r:12, border 1px #E8E8E6, p:24px)
      FormTitle 'Alterar Registro -- {codigo}' (18px 700 #111111)
      FieldGrid (display grid, grid-template-columns 1fr 1fr, gap:16px, mt:20px)
        CampoEditavel: Label (10px 700 uppercase) + Input (h:42, r:8, border 1px #E8E8E6)
        CampoReadonly: Label + ReadOnlyField (h:42, r:8, fill #F8F8F6, border 1px #F0F0EE)
          Valor (14px 500 #888888) + LockIcon (14x14, stroke #AAAAAA)
          Tooltip: 'Este campo nao pode ser editado para o status atual do registro.'
      ValidationResults (mt:20px, r:8, border 1px #E8E8E6, p:16px)
        Titulo 'Resultados da Validacao' (14px 700 #111111)
        AlertRow (flex, gap:8px, p:8px, r:6, bg #FFF8E1) IconeWarning + msg (12px 500 #E67E22)
        SummaryRow '{N} alertas . {M} erros bloqueantes' (12px 400 #888888)
      FormFooter (mt:20px, flex justify-end, gap:12px)
        BtnCancelar (h:40, r:8, border 1px #E8E8E6) 'Cancelar'
        BtnSalvar (h:40, r:8, fill #2E86C1) 'Salvar' (Disabled se blocking errors)
    MetadataSidebar (w:280, fill #FFF, r:12, border 1px #E8E8E6, p:20px, flex-shrink:0)
      SidebarTitle 'Metadata' (14px 700 #111111)
      MetaItem: STATUS — Badge (r:4, p:2px 8px, 10px 700)
      MetaItem: CRIADO EM — valor (12px 400 #555555)
      MetaItem: ATUALIZADO EM — valor
      MetaItem: ENQUADRADOR — valor
```

### Jornada (Happy Path)

1. Usuário navega para `/{modulo}/{rotina}/{id}/alterar`
2. Tela exibe skeleton enquanto avalia se registro pode ser editado (action: `evaluate_record_on_open`)
3. Motor avalia com `current_record_state` (F01 amendment)
4. Se sem `blocking_validations`: formulário renderiza com campos editáveis (visible_fields não bloqueados) e campos readonly (com cadeado)
5. Usuário altera campos editáveis
6. Usuário aciona "Validar" (action: `validate_changes`)
7. Se sem `blocking_validations`: botão "Salvar" habilitado
8. Usuário aciona "Salvar" (action: `save_changes`)
9. Registro alterado no módulo destino. Domain event com `changes[]` emitido.
10. Feedback de sucesso + redirecionamento para lista

### Alternativas e Cenários de Erro

| Cenário | Comportamento | Referência |
|---|---|---|
| Registro com status bloqueante | Componente BlockedRecordMessage exibido. Formulário NÃO abre. Botão "Voltar". | FR-006, BR-004 |
| Motor retorna erro (403/5xx) | Toast com mensagem de erro + `correlation_id`. Formulário não carrega. | FR-006 |
| Validação retorna `blocking_validations` | Mensagens inline por campo. Save desabilitado. | FR-006 |
| Validação retorna `validations` sem blocking | Mensagens de alerta inline. Save habilitado. | FR-006 |
| Erro ao salvar (409/422/5xx) | Mensagem inline + `correlation_id`. | FR-006 |
| Campo bloqueado por condição do motor | Campo renderizado como readonly com ícone de cadeado (não oculto). | BR-005, FR-006 |
| Cancelar e voltar | Navegação para `/{modulo}/{rotina}`. Sem modal de confirmação (formulário simples). | — |

### Estados da Tela

| Estado | Visual | Condição |
|---|---|---|
| Loading | Skeleton (formulário completo) | Avaliando registro via motor |
| Blocked | BlockedRecordMessage + botão "Voltar" | Motor retornou `blocking_validations` no open |
| Editing | Formulário com campos editáveis e readonly | Registro liberado para edição |
| Validating | Button loading no "Validar" | Validação em andamento |
| Validated (errors) | Mensagens inline por campo. Save desabilitado. | Validação com blocking |
| Validated (OK) | Save habilitado. | Validação sem blocking |
| Saving | Button loading no "Salvar" | Save em andamento |
| Error (mount) | Toast + formulário vazio | Falha ao avaliar registro |

### Ações Selecionadas (do DOC-UX-010)

| action_key | kind | scope | operation_ids | endpoints | domain_events | telemetria |
|---|---|---|---|---|---|---|
| `update` | command | single | `routine_engine_evaluate`, operationId do módulo destino | POST /api/v1/routine-engine/evaluate, PUT/PATCH /{modulo}/{entity}/{id} | `{modulo}.{entity}.updated`, `param.routine_applied` | UIActionEnvelope com `screen_id: UX-SGR-002` |
| `view` | query | single | `routine_engine_evaluate` | POST /api/v1/routine-engine/evaluate | `param.routine_applied` | UIActionEnvelope |

### Mapeamento Ações do Manifest → DOC-UX-010

| action_id (manifest) | action_key (DOC-UX-010) | Tipo | Notas |
|---|---|---|---|
| `evaluate_record_on_open` | `view` (query) | API | Avalia com `current_record_state` |
| `validate_changes` | custom (validação) | API | Revalida com dados alterados |
| `save_changes` | `update` (command) | API | Persiste no módulo destino |
| `navigate_back` | — (navegação) | Client | Voltar para lista |

### Componentes (do Manifest)

| component_id | Componente | Descrição | Ações usadas |
|---|---|---|---|
| `blocked-record-message` | BlockedRecordMessage | Exibido quando `evaluate_record_on_open` retorna `blocking_validations`. Mensagem de bloqueio + botão "Voltar". Conditional: `blocking_validations não vazio`. | `navigate_back` |
| `edit-form` | SmartEditForm | Formulário com campos editáveis (visible_fields não bloqueados), campos readonly com cadeado, domain dropdowns, required asterisco, defaults pré-preenchidos. Botões: "Cancelar", "Validar", "Salvar". | `evaluate_record_on_open`, `validate_changes`, `save_changes`, `navigate_back` |

### Copy (Mensagens)

| Contexto | Mensagem (pt-BR) |
|---|---|
| Sucesso (save_changes) | "Registro alterado com sucesso." |
| Erro (save_changes) | "Não foi possível salvar as alterações." |
| Erro (evaluate_record_on_open) | "Não foi possível verificar o registro." |
| Erro (validate_changes) | "Não foi possível validar as alterações." |
| Registro bloqueado | "Este registro não pode ser editado. Motivo: {blocking_validation_message}" |
| Campo readonly | Tooltip: "Este campo não pode ser editado para o status atual do registro." |

---

## UX-SGR-003 — Grade de Exclusão em Massa

- **Screen ID:** UX-SGR-003
- **Entidade(s) principal(is):** `{entity_type}` do módulo destino (dinâmico)
- **Contexto:** Grade de validação pré-exclusão e confirmação de exclusão lógica em massa
- **Features:** US-MOD-011-F04

### Layout Visual (ref: UX-011-M01, spec 95-smartgrid)

```
ContentArea (1200x836, fill #F5F5F3, padding:24px)
  PageHeader (flex justify-between, align-center)
    Esquerda: 'Exclusao em Massa' (24px 800 #111111) + '{N} registros selecionados' (13px 400 #888888)
    BtnVerificar (primary: r:8, fill #2E86C1, h:40) 'Verificar' (13px 700 #FFFFFF)
  GridCard (fill #FFF, r:12, border 1px #E8E8E6, mt:16px)
    GridHeader: STATUS (w:60) | CODIGO (w:120) | DESCRICAO (flex:1) | RESULTADO (w:200)
    GridRow (h:48): StatusIcon + Codigo + Descricao + Resultado
      Pode ser excluido: (12px 500 #1E7A42)
      Bloqueado: (12px 500 #E74C3C) 'Bloqueado: {motivo}'
```

### Jornada (Happy Path)

1. Usuário seleciona registros na lista do módulo destino e navega para `/{modulo}/{rotina}/exclusao-em-massa`
2. Tela exibe lista dos registros selecionados (componente: SelectionList)
3. Usuário aciona "Verificar" — motor chamado por registro com `current_record_state` (action: `validate_for_delete`)
4. Registros classificados: liberados (sem blocking) em verde, bloqueados (com motivo) em vermelho
5. Componente DeleteConfirmationPanel exibe resumo: "{X} liberados para exclusão, {Y} bloqueados"
6. Usuário aciona "Confirmar exclusão ({X} registros)" (action: `confirm_delete`)
7. Exclusão lógica executada (`deleted_at` + `status=INACTIVE`) nos registros liberados
8. Componente DeleteResultFeedback exibe resultado por registro: ✅ excluído / ❌ falhou
9. Toast com contagem final

### Alternativas e Cenários de Erro

| Cenário | Comportamento | Referência |
|---|---|---|
| Todos os registros bloqueados | DeleteConfirmationPanel exibe todos em vermelho. Botão "Confirmar exclusão" desabilitado. | FR-007 |
| Cancelar antes da confirmação | Navegação para lista. Nenhum registro alterado. | FR-007 |
| Erro na verificação de 1 registro | Registro marcado como "erro" (ícone ❌) com mensagem. Demais continuam. | FR-007 |
| Erro ao excluir (parcial) | DeleteResultFeedback exibe mix de ✅ e ❌. Toast com contagem. | FR-007 |
| Motor retorna erro (403/5xx) | Toast com mensagem de erro + `correlation_id`. | FR-007 |
| Registro já excluído (409) | Registro marcado como "já excluído" no feedback. | FR-007 |

### Estados da Tela

| Estado | Visual | Condição |
|---|---|---|
| Loaded | SelectionList com registros selecionados + botão "Verificar" | Tela aberta, registros carregados |
| Verifying | Spinner global ou por registro | Validação em andamento |
| Verified | DeleteConfirmationPanel com liberados (verde) e bloqueados (vermelho) | Verificação completa |
| Confirming | Button loading no "Confirmar exclusão" | Exclusão em andamento |
| Completed | DeleteResultFeedback com resultado por registro | Exclusão finalizada |
| Error (verify) | Toast + registros não classificados | Falha na verificação |

### Ações Selecionadas (do DOC-UX-010)

| action_key | kind | scope | operation_ids | endpoints | domain_events | telemetria |
|---|---|---|---|---|---|---|
| `delete` | command | bulk | `routine_engine_evaluate`, operationId do módulo destino | POST /api/v1/routine-engine/evaluate, DELETE /{modulo}/{entity}/{id} | `{modulo}.{entity}.deleted`, `param.routine_applied` | UIActionEnvelope com `screen_id: UX-SGR-003` |

### Mapeamento Ações do Manifest → DOC-UX-010

| action_id (manifest) | action_key (DOC-UX-010) | Tipo | Notas |
|---|---|---|---|
| `validate_for_delete` | custom (validação pré-delete) | API | Verifica cada registro com `current_record_state` |
| `confirm_delete` | `delete` (command, bulk) | API | Exclusão lógica dos registros liberados |
| `navigate_back` | — (navegação) | Client | Cancelar e voltar para lista |

### Componentes (do Manifest)

| component_id | Componente | Descrição | Ações usadas |
|---|---|---|---|
| `selection-list` | SelectionList | Lista dos registros selecionados para exclusão. Botão "Verificar" dispara `validate_for_delete` para todos. | `validate_for_delete` |
| `delete-confirmation-panel` | DeleteConfirmationPanel | Seção com registros liberados (verde) e bloqueados (vermelho com motivo). Botões: "Cancelar" / "Confirmar exclusão ({X} registros)". | `confirm_delete`, `navigate_back` |
| `delete-result-feedback` | DeleteResultFeedback | Feedback final: ✅ excluído / ❌ falhou (com motivo). Toast com contagem total. | — |

### Copy (Mensagens)

| Contexto | Mensagem (pt-BR) |
|---|---|
| Sucesso (confirm_delete total) | "{X} registros excluídos com sucesso." |
| Sucesso (confirm_delete parcial) | "{X} registros excluídos. {Y} falharam." |
| Erro (validate_for_delete) | "Não foi possível verificar o registro." |
| Erro (confirm_delete) | "Não foi possível excluir os registros." |
| Todos bloqueados | "Nenhum registro pode ser excluído no status atual." |
| Registro bloqueado (motivo) | "Registro bloqueado: {blocking_validation_message}" |
| Registro já excluído | "Registro já foi excluído anteriormente." |
| Confirmação | "Você está prestes a excluir {X} registros. Esta ação é irreversível. Deseja continuar?" |

---

## Componentes Adicionais (ref: UX-011-M01)

### DeleteConfirmationPanel (split-view)

```
ConfirmationOverlay (fixed, inset 0, bg rgba(0,0,0,0.3), z:50, flex center)
  ConfirmationCard (w:720, r:12, fill #FFF, shadow 0 8px 32px rgba(0,0,0,0.12))
    Header 'Confirmacao de Exclusao' (18px 700 #111111)
    SplitView (flex, min-h:300px)
      BlockedPanel (flex:1, p:20px, bg #FFEBEE, border-right 1px #F0F0EE)
        'Nao podem ser excluidos ({N})' (14px 700 #E74C3C)
        RecordItem (p:12px, r:8, bg #FFF, border 1px #F5C6CB) Nome + Motivo
      ReadyPanel (flex:1, p:20px, bg #E8F8EF)
        'Prontos para exclusao ({N})' (14px 700 #27AE60)
        RecordItem (p:12px, r:8, bg #FFF, border 1px #B5E8C9) Nome
    Footer: BtnCancelar + BtnExcluir (fill #E74C3C) 'Excluir {N} registros'
```

### DeleteResultFeedback

```
FeedbackCard (w:480, r:12, fill #FFF, p:24px)
  SuccessSection (bg #E8F8EF): CheckIcon + '{N} registros excluidos com sucesso' (14px 600 #1E7A42)
  FailureSection (bg #FFEBEE): XIcon + '{N} registros nao excluidos' (14px 600 #E74C3C)
    FailItem '{codigo} -- {motivo}' (12px 400 #888888)
  BtnFechar (h:40, r:8, border 1px #E8E8E6)
```

### ImportJsonModal (novo componente)

```
ModalCard (w:520, r:12, fill #FFF, p:24px)
  Titulo 'Importar JSON' (18px 700 #111111)
  DropZone (mt:16px, h:160, r:8, border 2px dashed #E8E8E6, flex-col center)
    IconeUpload + 'Arraste um arquivo ou clique para selecionar' (13px 500 #888888)
    '.json (max. {MAX} linhas)' (11px 400 #CCCCCC)
  [Apos upload]
    SchemaOK: CheckIcon + 'Schema valido -- {N} linhas detectadas' (#1E7A42)
    SchemaFail: XIcon + 'Schema invalido: {motivo}' (#E74C3C)
  BtnImportar (fill #2E86C1, Disabled se schema invalido)
```

### ReadOnlyField (componente reutilizável)

```
Path: apps/web/src/shared/ui/readonly-field.tsx
Props: value: string, label?: string, tooltip?: string
Render: Container (h:42, r:8, fill #F8F8F6, border 1px #F0F0EE, flex align-center justify-between)
  Valor (14px 500 #888888) + LockIcon SVG (14x14, stroke #AAAAAA)
```

### Row Status Indicators (border-left + StatusIcon)

| Status | border-left | bg row | StatusIcon |
|---|---|---|---|
| VALID | 3px solid #27AE60 | #E8F8EF | check-circle verde (aria-label 'Linha valida') |
| WARNING | 3px solid #F39C12 | #FFF8E1 | alert-triangle ambar (aria-label 'Linha com alerta') |
| ERROR | 3px solid #E74C3C | #FFEBEE | x-circle vermelho (aria-label 'Linha com erro bloqueante') |

### CloseConfirmationModal (detalhe visual)

```
ModalCard (w:420, r:12) IconeWarning + 'Alteracoes nao salvas'
  BtnCancelar | BtnExportar (border #2E86C1) 'Exportar e sair' | BtnSair (fill #E74C3C) 'Sair sem exportar'
```

---

## Empty, Error e Loading States (ref: UX-011-M01)

### Empty — UX-SGR-001

```
EmptyContainer (flex-col, align-center, p:60px)
  Ilustracao (w:120, h:120, icone grid, stroke #CCCCCC)
  'Nenhuma linha adicionada.' (16px 600 #888888)
  'Clique em Adicionar linha para comecar.' (13px 400 #AAAAAA)
  BtnAdicionar (primary) '+ Adicionar primeira linha'
```

### Loading Skeleton — UX-SGR-001

```
GridCard: GridHeader (normal) + SkeletonRows (6x) — Rects por coluna, r:4 bg:#E8E8E6 pulse
```

### Loading Skeleton — UX-SGR-002

```
FormCard: Rect titulo + Grid 2col x 6 FieldSkeleton (label + input)
```

### Error State

```
ErrorContainer (flex-col, align-center, p:40px)
  alert-triangle (w:48, stroke #E74C3C)
  'Nao foi possivel carregar a configuracao da Operacao.' (14px 500 #888888)
  BtnRetry 'Tentar novamente'
```

---

## Responsividade (ref: UX-011-M01)

| Breakpoint | Comportamento |
|---|---|
| >= 1280px | Grade full-width, todas colunas visíveis, formulário com sidebar metadata |
| 1024-1279px | Grade com scroll horizontal, formulário sem sidebar (metadata abaixo do form) |
| 768-1023px | Grade compacta (3 colunas visíveis + scroll), formulário single-column, modais 100% width |
| < 768px | Grade em cards verticais (1 card por registro), formulário full-screen, DeleteConfirmationPanel em stack vertical |

---

## Telemetria Comum (todas as telas)

Todas as telas usam o pacote `@easycf/ui-telemetry` com os defaults definidos nos Screen Manifests:

| Configuração | Valor |
|---|---|
| `package` | `@easycf/ui-telemetry` |
| `include_tenant_id` | `true` |
| `propagate_correlation_id` | `true` |
| `capture_duration_ms` | `true` |

Cada ação emite um UIActionEnvelope (DOC-ARC-003) com campos obrigatórios: `screen_id`, `action`, `entity_type`, `tenant_id`, `correlation_id`, `status`, `duration_ms`.

---

## Navegação e Guards

| Tela | Guard | Redirect (unauth) | Back action |
|---|---|---|---|
| UX-SGR-001 | `auth` | `/login` | — |
| UX-SGR-002 | `auth` | `/login` | `/{modulo}/{rotina}` |
| UX-SGR-003 | `auth` | `/login` | `/{modulo}/{rotina}` |

---

## Acessibilidade (a11y)

- **MUST:** Grade (UX-SGR-001) acessível via teclado: Tab para navegar entre cells, Enter para editar, Escape para cancelar.
- **MUST:** Ícones de status (✅ ❌ ⚠️) DEVEM ter `aria-label` descritivo (ex: "Linha válida", "Linha com erro bloqueante", "Linha com alerta").
- **MUST:** Modal de confirmação (CloseConfirmationModal, DeleteConfirmationPanel) acessível: focus trap, Escape para fechar.
- **MUST:** Mensagens de erro inline associadas ao campo via `aria-describedby`.
- **Referência:** NFR-010

---

## Referências

- **DOC-UX-010:** Catálogo de Ações Padrão (action_keys utilizados)
- **DOC-ARC-003:** UIActionEnvelope e telemetria
- **DOC-FND-000:** Contratos Fundacionais (auth guard, RBAC)
- **FR-011:** Requisitos Funcionais (referências por FR-xxx)
- **BR-011:** Regras de Negócio de interface
- **SEC-011:** Segurança e Compliance (client-side security)
- **NFR-011:** Requisitos Não Funcionais (performance, limites, a11y)
- **DATA-003:** Catálogo de Domain Events (eventos rastreados)

---

<!-- Enriquecimento: AGN-DEV-07 completo -->

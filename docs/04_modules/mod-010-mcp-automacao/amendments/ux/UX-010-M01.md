> **ARQUIVO GERIDO POR AUTOMACAO.**
> - Emenda sobre documento normativo/requisito em estado READY.
> - Para novas emendas, use a skill `create-amendment`.

# Emenda: UX-010-M01

- **Documento base:** [UX-010](../../requirements/ux/UX-010.md)
- **estado_item:** MERGED
- **Natureza:** M (Melhoria)
- **Data:** 2026-03-31
- **owner:** Marcos Sulivan
- **Motivacao:** Alinhar componentes React de AgentsPage (UX-MCP-001) e ExecutionsPage (UX-MCP-002) com os designs validados no Penpot sandbox (7 frames: 60-MCP-Agents, 60-MCP-Catalogo, 60-MCP-Permissoes, 60-MCP-Monitor, 60-MCP-Drawer, 60-MCP-Modal-ApiKey, 60-MCP-Modal-Revogar). O layout atual usa tabelas genericas com PageHeader pill-tabs; o design validado exige tab bar com estilo A1, catalogo em cards grid, drawer com labels uppercase, metric cards com progress bar, e detail panel com secoes agrupadas.
- **rastreia_para:** 60-mcp-agents-spec.md, 60-mcp-agents-ref_claude-desktop.html, UX-MCP-001, UX-MCP-002, ux-mcp-001.gestao-agentes.yaml, ux-mcp-002.monitor-execucoes.yaml, Penpot frames 60-MCP-*

---

## Detalhamento

### D1 — Tab Bar A1 (AgentsPage)

O AgentsPage DEVE substituir a pill-nav no PageHeader por tab bar estilizada abaixo do header:

```
PageHeader (flex justify-between, align-center)
  Esquerda
    "MCP Agentes" (24px 800 #111111)
    "Gerencie agentes MCP, ações e permissões" (13px 400 #888888, mt:4px)
  Direita
    BtnCriar (r:8, fill #2E86C1, h:40, p:0 20px)
      "+ Criar Agente" (13px 700 #FFFFFF)

TabBar (flex, gap:0, mt:20px, border-bottom 2px #E8E8E6)
  Tab ativo  → bg #2E86C1, text #FFFFFF, r:6 6 0 0, p:10px 20px, font 13px 700
  Tab inativo → bg transparent, text #888888, p:10px 20px, font 13px 500
```

- Titulo: mudar de "MCP — Agentes e Ações" para "MCP Agentes"
- Botao "+ Criar Agente" SAI do FilterBar e vai para o PageHeader (direita)
- Tab labels: "Agentes", "Catálogo de Ações", "Permissões"
- Remover pill-nav (segment control) existente

### D2 — Toolbar com SearchBar (AgentsTab)

Abaixo da tab bar, o painel Agentes DEVE ter toolbar com busca e filtros:

```
ToolBar (flex justify-between, align-center, mt:16px)
  SearchBar (w:320, h:40, r:8, border 1px #E8E8E6, fill #FFFFFF)
    IconeLupa (16x16, stroke #CCCCCC, x:12)
    Placeholder "Buscar por nome ou código..." (13px 400 #CCCCCC, x:38)
  FilterGroup (flex, gap:8px)
    FilterStatus (select: "Todos os Status", w:180, h:40, r:8)
    FilterOwner (select: "Todos os Owners", w:180, h:40, r:8)
```

- Adicionar SearchBar com icone de lupa
- Adicionar filtro por owner (novo filtro, requer endpoint com `?owner_user_id=`)
- Manter filtro por status existente

### D3 — AgentsTable Colunas (atualizado)

A tabela de agentes DEVE ter estas colunas, conforme spec:

```
TableHeader (h:44, bg #FAFAFA, border-bottom 1px #F0F0EE, p:0 20px)
  "CÓDIGO"  (w:120, font 10px 700 uppercase ls:+0.8px #888888)
  "NOME"    (w:flex)
  "TIPO"    (w:100)
  "STATUS"  (w:100)
  "API KEY" (w:140)
  "AÇÕES"   (w:100, text-align center)

TableRow (h:52, border-bottom 1px #F0F0EE, p:0 20px, hover bg #F8F8F6)
  Código    → "AGENT-01" (13px 600 #333333)
  Nome      → "Agente Comercial" (13px 500 #111111)
  Tipo      → Badge cinza "AUTOMATION" (10px 700 uppercase, text #6C757D, bg #F4F4F2)
  Status    → Badge colorido (ACTIVE=verde, REVOKED=vermelho, PENDING=ambar, INACTIVE=cinza)
  API Key   → "sk-...a3f4" (12px 400 #888888, font-family monospace)
  Ações     → 3 icones: Editar (pencil), Rotacionar Key (key), Revogar (shield-off)
```

Mudancas vs atual:
- REMOVER coluna "Phase 2"
- REMOVER coluna "Ultimo uso"
- ADICIONAR coluna "TIPO" com badge
- ADICIONAR coluna "API KEY" com hint monospace
- Icones de acao: trocar botoes texto por icones SVG 16x16
- Agente REVOKED: opacity 0.5 na linha inteira (ja implementado como 0.6, ajustar)
- Header: labels UPPERCASE com letter-spacing 0.8px

Badge styles (cores da spec):
- ACTIVE: text #1E7A42, bg #E8F8EF, border #B5E8C9, r:4
- REVOKED: text #C0392B, bg #FFEBEE, border #F5C6CB
- PENDING: text #B8860B, bg #FFF3E0, border #FFE0B2
- INACTIVE: text #888888, bg #F5F5F3, border #E8E8E6

### D4 — Catálogo de Ações em Cards Grid (atualizado)

O painel "Catálogo de Ações" DEVE exibir cards em grid 3 colunas em vez de tabela:

```
ToolBar (flex justify-between, mt:16px)
  SearchBar (w:320, h:40, placeholder "Buscar ação...")
  FilterGroup
    FilterTipo (select: "Todos os Tipos", w:180)
    FilterPolitica (select: "Todas as Políticas", w:180)
  BtnCriarAcao (secondary: r:8, border #2E86C1, h:36)
    "+ Nova Ação" (13px 600 #2E86C1)

ActionsCatalog (grid 3 colunas, gap:16px, mt:16px)
  ActionCard (fill #FFFFFF, r:12, border 1px #E8E8E6, p:20px)
    HeaderRow (flex justify-between)
      Código "process:case:create" (11px 600 #333333, monospace)
      PolicyBadge "DIRECT" (badge verde)
    Nome "Criar Processo" (16px 600 #111111, mt:8px)
    Descrição "Cria um novo processo..." (12px 400 #888888, mt:4px)
    ScopesRow (flex, gap:4px, mt:12px, flex-wrap)
      ScopeChip "process:case:write" (10px 500 #555555, bg #F5F5F3, r:4, p:2px 8px)
    FooterRow (flex justify-end, mt:12px)
      BtnEdit (pencil 14x14, stroke #888888, hover #2E86C1)
```

Policy badge styles:
- DIRECT: text #1E7A42, bg #E8F8EF, border #B5E8C9
- CONTROLLED: text #B8860B, bg #FFF3E0, border #FFE0B2
- EVENT_ONLY: text #6C757D, bg #F4F4F2, border #E0E0DE

Mudancas vs atual:
- Trocar Table por grid de ActionCards
- Adicionar descricao e scope chips
- Botao "+ Nova Ação" como secondary (outline) na toolbar, nao primary

### D5 — Permissions Matrix Estilizada (atualizado)

A PermissionsMatrix DEVE ter header com nomes de acao rotacionados e checkboxes estilizados:

```
PermissionsMatrix (fill #FFFFFF, r:12, border 1px #E8E8E6, overflow auto)
  MatrixHeader (sticky top:0, bg #F8F8F6)
    Cell vazia (w:200, h:80)
    ActionHeader "process:case:create" (w:60, h:80, text rotated -45deg, 10px 600 #888888)
    ...

  MatrixRow (h:44, border-bottom 1px #F0F0EE)
    AgentLabel "AGENT-01 — Agente Comercial" (w:200, 12px 600 #333333, p:0 12px)
    Checkbox (16x16, checked: fill #2E86C1 r:4, unchecked: border #E8E8E6 r:4)
    ...
  [Agente REVOKED: linha desabilitada, checkboxes disabled, opacity 0.4]
```

Mudancas vs atual:
- Agente label: mostrar "CÓDIGO — Nome" em vez de apenas codigo
- Action headers: texto rotacionado -45deg para caber mais colunas
- Checkbox estilizado: checked = fill azul (#2E86C1), unchecked = borda cinza
- Agentes REVOKED: incluir na lista com opacity 0.4 e checkboxes disabled (atualmente filtrados)
- Header fixo (sticky) para scroll vertical

### D6 — CreateAgentDrawer Layout (atualizado)

O drawer de criacao DEVE seguir layout especifico:

```
DrawerPanel (480px x 100vh, fill #FFFFFF, right:0, shadow -4px 0 24px rgba(0,0,0,0.08))
  DrawerHeader (h:64, border-bottom 1px #E8E8E6, p:0 24px, flex align-center justify-between)
    "Criar Agente" ou "Editar Agente" (18px 700 #111111)
    BtnFechar (X 20x20, stroke #888888, hover #333333)

  DrawerBody (flex-1, overflow-y auto, p:24px)
    CampoCodigo
      Label "CÓDIGO" (10px 700 uppercase ls:+0.8px #888888, mb:6px)
      Input (w:100%, h:42, r:8, border #E8E8E6, p:10px 14px, font 14px 500 #111111)
      [Modo edição: ReadOnlyField — fill #F8F8F6, border #F0F0EE, cursor default]
      [Erro] "Já existe um agente com este código neste tenant." (11px 500 #E74C3C, mt:4px)

    CampoNome (mt:16px) → mesma estrutura
    CampoOwner (mt:16px) → Autocomplete com placeholder "Buscar usuário..."
    CampoScopes (mt:16px)
      Label "ESCOPOS PERMITIDOS"
      MultiSelect (w:100%, min-h:42, r:8, border #E8E8E6)
        ScopeChips (flex wrap, gap:4px)
          Chip "process:case:read" (bg #E3F2FD, text #2E86C1, r:4, p:2px 8px, × close)
      [Erro escopo bloqueado] (11px 500 #E74C3C)

    [Banner REVOKED — modo edicao de agente revogado]
      AlertBanner (fill #FFEBEE, border 1px #F5C6CB, r:8, p:12px)
        "Agente revogado — somente leitura." (13px 500 #C0392B)

  DrawerFooter (h:72, border-top 1px #E8E8E6, p:16px 24px, flex justify-end gap:12px)
    BtnCancelar (h:40, r:8, border #E8E8E6, p:0 20px, bg #FFFFFF)
      "Cancelar" (13px 600 #555555)
    BtnSalvar (h:40, r:8, fill #2E86C1, p:0 20px)
      "Salvar" (13px 700 #FFFFFF)
```

Mudancas vs atual:
- Labels: UPPERCASE com letter-spacing (atualmente plain text)
- Drawer width: definir 480px max via className
- Footer: botoes alinhados a direita com gap (atualmente stacked)
- Scope chips: mudar de Badge secondary cinza para chips azul claro (#E3F2FD text #2E86C1)
- Adicionar modo edicao (codigo readonly, banner REVOKED)
- Owner: trocar Input simples por Autocomplete com busca de usuarios

### D7 — ApiKeyModal Design (atualizado)

O ApiKeyModal DEVE seguir layout centered com icone de chave:

```
ModalCard (w:520, r:12, fill #FFFFFF, shadow 0 8px 32px rgba(0,0,0,0.12), p:24px)
  IconeKey (48x48, r:50%, bg #FFF3E0, stroke #F39C12, centrado)
  Título "Chave de API Gerada" (18px 700 #111111, mt:16px, text-align center)
  Aviso "Esta chave será exibida apenas uma vez. Copie-a agora." (13px 600 #E74C3C, text-align center)

  ApiKeyField (mt:16px, fill #F8F8F6, r:8, p:14px, flex justify-between, border 1px #E8E8E6)
    Key "sk-mcp-..." (14px 500 #111111, monospace, user-select:all)
    BtnCopiar (p:6px 12px, r:6, border #2E86C1, bg transparent)
      "Copiar" (12px 700 #2E86C1) → após: "Copiado!" (12px 700 #27AE60)

  CheckboxRow (mt:16px, flex align-center gap:8px)
    Checkbox (16x16, r:4, unchecked: border #E8E8E6, checked: fill #2E86C1)
    "Copiei e armazenei a chave com segurança" (13px 500 #333333)

  BtnFechar (mt:20px, w:100%, h:40, r:8)
    Habilitado: fill #2E86C1, text #FFFFFF
    Desabilitado: fill #E8E8E6, text #CCCCCC
    [Tooltip quando desabilitado: "Confirme que copiou a chave antes de fechar."]
```

Mudancas vs atual:
- Adicionar icone de chave (48x48 com fundo ambar)
- Titulo: centrado, mudar de "API Key Gerada" para "Chave de API Gerada"
- API key field: fundo #F8F8F6 com borda (atualmente bg-muted generico)
- Botao copiar: outline azul (atualmente outline generico)
- Botao fechar: full-width, azul quando habilitado, cinza quando desabilitado
- Modal width: 520px (sm:max-w-lg → max-w-[520px])

### D8 — RevokeModal Design (atualizado)

O RevokeModal DEVE ter icone de warning e textarea para motivo:

```
ModalCard (w:480, r:12, fill #FFFFFF, shadow 0 8px 32px rgba(0,0,0,0.12), p:24px)
  IconeWarning (48x48, r:50%, bg #FFEBEE, stroke #E74C3C, centrado)
  Título "Revogar agente?" (18px 700 #111111, mt:16px, text-align center)
  Mensagem "Revogar o agente {nome}? Esta ação é irreversível." (13px 400 #555555, text-align center)

  CampoMotivo (mt:16px)
    Label "MOTIVO DA REVOGAÇÃO" (10px 700 uppercase ls:+0.8px #888888, mb:6px)
    Textarea (w:100%, h:80, r:8, border #E8E8E6, p:10px 14px)
      Placeholder "Informe o motivo (mínimo 10 caracteres)..." (14px 400 #CCCCCC)

  BotõesRow (mt:20px, flex justify-center, gap:12px)
    BtnCancelar (h:40, r:8, border #E8E8E6, p:0 20px)
      "Cancelar" (13px 600 #555555)
    BtnRevogar (h:40, r:8, fill #E74C3C, p:0 20px)
      "Revogar definitivamente" (13px 700 #FFFFFF)
      [Desabilitado se motivo < 10 caracteres]
```

Mudancas vs atual:
- Adicionar icone warning (48x48, fundo vermelho claro)
- Titulo: centrado, mudar de "Confirmar Revogacao" para "Revogar agente?"
- Trocar Input por Textarea (h:80) para motivo
- Label UPPERCASE com letter-spacing
- Botoes centralizados (atualmente align-end em DialogFooter)
- Modal width: 480px

### D9 — Monitor MetricCards (atualizado)

Os metric cards da ExecutionsPage DEVEM seguir o design:

```
MetricCards (grid 4 colunas, gap:16px, mt:20px)
  Card (fill #FFFFFF, r:12, border 1px #E8E8E6, p:20px)
    Label (11px 600 uppercase ls:+0.5px #888888)
    Valor (28px 800 #111111)

  Card "Taxa de Sucesso" → adicionar barra de progresso:
    Indicador (barra 100%x4, r:2, bg #F0F0EE, fill proporcion. cor por threshold)
    Thresholds: >95% = #27AE60 (verde), 80-95% = #F39C12 (ambar), <=80% = #E74C3C (vermelho)

  Card "Bloqueados" → se tem escalacoes:
    EscalationBadge "N Escaladas" (10px 700 #FFFFFF, bg #E74C3C, r:10, p:2px 8px, ml:8px)
```

Mudancas vs atual:
- Cards: trocar border-2 por border 1px com r:12
- Label: mudar para 11px uppercase com letter-spacing (atualmente text-sm)
- Valor: 28px 800 (atualmente text-2xl bold — similar, ajustar tamanho exato)
- Progress bar na taxa de sucesso (novo)
- Escalation badge no card Bloqueados (novo)
- Remover variante de cor na borda; manter cor apenas no valor e barra

### D10 — Monitor FilterBar (atualizado)

A barra de filtros do Monitor DEVE ser atualizada:

```
FilterBar (flex, gap:12px, mt:20px, align-center)
  SearchBar (w:280, h:40, r:8, placeholder "Buscar por correlation ID...")
  FilterAgente (select, w:180, h:40, placeholder "Agente")
  FilterAcao (select, w:180, h:40, placeholder "Ação")
  FilterStatus (multi-select, w:180, h:40, placeholder "Status")
  DateRange (flex, gap:4px)
    DateFrom (input date, w:140, h:40)
    DateTo (input date, w:140, h:40)
```

Mudancas vs atual:
- Busca: trocar 2 SearchBars (agent_id, action_id) por 1 SearchBar para correlation ID
- Filtros: usar Select para Agente e Acao (carregar opcoes dos endpoints list)
- Status: multi-select (atualmente single-select)
- Datas: trocar datetime-local por date com estilo consistente

### D11 — ExecutionsTable Colunas (atualizado)

A tabela de execucoes DEVE ter estas colunas:

```
TableHeader (h:44, bg #FAFAFA)
  "AGENTE"    (w:120, 10px 700 uppercase ls:+0.8px #888888)
  "AÇÃO"      (w:140)
  "STATUS"    (w:120)
  "INÍCIO"    (w:140)
  "DURAÇÃO"   (w:80)
  "RESULTADO" (w:flex)

TableRow (h:52, cursor pointer, hover bg #F8F8F6)
  Agente    → codigo do agente "AGENT-01" (13px 600 #333333)
  Ação      → codigo da acao "process:case:create" (13px 500 #111111, monospace)
  Badge status → colorido conforme D3
  [Escalation row: border-left 3px #E74C3C + EscalationBadge "Escalada"]
  Início    → "31/03/2026 14:32:01" (12px 400 #888888)
  Duração   → "245ms" (12px 400 #888888)
  Resultado → "Processo criado: PROC-0042" (12px 400 #888888, truncate)
```

Mudancas vs atual:
- Mostrar codigo do agente (atualmente mostra agent_id truncado)
- Mostrar codigo da acao (atualmente action_id truncado)
- REMOVER coluna "Politica"
- ADICIONAR coluna "RESULTADO" com resultado truncado
- RENOMEAR "Recebido em" para "INÍCIO"
- Headers UPPERCASE com letter-spacing

### D12 — ExecutionDetailPanel Secoes (atualizado)

O painel de detalhe DEVE ser reorganizado em secoes com headers UPPERCASE:

```
DetailPanel (w:40%, fill #FFFFFF, r:0 12 12 0, border 1px #E8E8E6)
  DetailHeader (h:56, p:0 20px, flex justify-between, border-bottom 1px #E8E8E6)
    "Detalhe da Execução" (16px 700 #111111)
    BtnFechar (X 18x18, stroke #888888)

  [EscalationAlert — se privilege_escalation]
    AlertBox (fill #FFEBEE, border 1px #F5C6CB, r:8, p:16px)
      "TENTATIVA DE ESCALADA DE PRIVILÉGIO" (11px 700 #C0392B, uppercase)
      "Escopo tentado: {scope}" (12px 400 #C0392B, mt:4px)

  StatusBadge grande (14px 700, p:6px 16px, r:6)

  Seção "AGENTE" (mt:20px)
    SectionTitle (10px 700 uppercase ls:+0.8px #888888, mb:8px)
    Campo "Código" → valor (flex between, h:28)
    Campo "Nome" → valor
    Campo "Owner" → valor

  Seção "AÇÃO" (mt:16px)
    SectionTitle "AÇÃO"
    Campo "Código" → monospace
    Campo "Política" → Badge colorido
    Campo "Tipo" → "write"/"read"

  Seção "EXECUÇÃO" (mt:16px)
    SectionTitle "EXECUÇÃO"
    Campo "Correlation ID" → monospace + icone copiar
    Campo "Início" → data/hora
    Campo "Duração" → "245ms"
    Campo "Status" → Badge

  Seção "PAYLOAD" (mt:16px, accordion)
    SubSeção "Request" → JSONViewer (bg #F8F8F6, r:8, p:12px, monospace 12px)
    SubSeção "Response" → JSONViewer (mesma estrutura)
    [Campos sensiveis mascarados com "***"]

  [Footer — se CONTROLLED_PENDING]
    BtnVerInbox (primary, w:100%, h:40, mt:16px)
      "Ver no inbox de aprovações" (13px 700 #FFFFFF)
```

Mudancas vs atual:
- Reorganizar de dl/dt/dd flat para secoes agrupadas (AGENTE, AÇÃO, EXECUÇÃO, PAYLOAD)
- Section titles: UPPERCASE com letter-spacing
- Payload: trocar `<details>` por accordion customizado com estilo
- JSON viewer: fundo #F8F8F6 com border-radius (atualmente bg-muted generico)
- Escalation alert: box dedicada em vez de inline div
- Status badge: tamanho grande (14px padding)
- Botao "Ver no inbox": full-width primary (atualmente outline sm)

---

## Impacto nos Pilares

- **DATA:** Sem impacto — campos ja existem no schema (tipo agente, api_key hint, resultado execucao)
- **FR:** Impacto menor — filtro por owner_user_id no endpoint GET /admin/mcp-agents (ja suportado como query param). Busca por correlation_id no endpoint GET /admin/mcp-executions (verificar se existe).
- **UX (manifests):** Atualizar ux-mcp-001.gestao-agentes.yaml e ux-mcp-002.monitor-execucoes.yaml com novos componentes (ActionCard, MetricCardProgress, SectionedDetailPanel)
- **BR:** Sem impacto
- **SEC:** Sem impacto

---

## Componentes

| Componente | Classificacao | Acao |
|---|---|---|
| AgentsPage (tab bar) | UPDATE | Substituir pill-nav por tab bar A1 |
| AgentsTab (toolbar) | UPDATE | Adicionar SearchBar + filtro owner |
| AgentsTable (colunas) | UPDATE | Novas colunas TIPO, API KEY; remover Phase2, Ultimo uso |
| ActionsTab (catalog) | UPDATE | Trocar Table por grid de ActionCards |
| ActionCard | NEW | Criar componente card para catalogo de acoes |
| MatrixTab (permissoes) | UPDATE | Rotated headers, styled checkboxes, incluir REVOKED |
| CreateAgentDrawer | UPDATE | Labels uppercase, scope chips azuis, modo edicao |
| ApiKeyModal | UPDATE | Icone, layout centered, full-width button |
| RevokeModal | UPDATE | Icone, textarea, layout centered |
| MetricCard (progress) | UPDATE | Progress bar, escalation badge |
| ExecutionsPage (filters) | UPDATE | Correlation ID search, select por agente/acao |
| ExecutionsTable (colunas) | UPDATE | Codigos em vez de IDs, coluna resultado |
| ExecutionDetailPanel | UPDATE | Secoes agrupadas, JSON viewer estilizado |

---

## Resolução do Merge

> **Merged por:** merge-amendment em 2026-03-31
> **Versão base após merge:** 0.3.0
> **Alterações aplicadas:** Incorporadas seções §2.8 (Layout Visual UX-MCP-001 com cores, tipografia e medidas), §3.8 (Layout Visual Monitor UX-MCP-002) e atualizadas §2.6 e §3.6 (Componentes Específicos) com detalhamento visual completo do Penpot (12 itens D1-D12).
